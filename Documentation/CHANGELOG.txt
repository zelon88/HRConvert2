-----------------------------------------------------------------------------------
COPYRIGHT INFORMATION ...
HRConvert2, Copyright on 8/24/2026 by Justin Grimes, www.github.com/zelon88

LICENSE INFORMATION ...
This project is protected by the GNU GPLv3 Open-Source license.
https://www.gnu.org/licenses/gpl-3.0.html

APPLICATION INFORMATION ...
This application is designed to provide a web-interface for converting file formats
on a server for users of any web browser without authentication. 

FILE INFORMATION ...
v3.8.0.
This file contains a list of itemized changes made in each iteration of the application.

HARDWARE REQUIREMENTS ... 
This application requires at least a Raspberry Pi Model B+ or greater.
This application will run on just about any x86 or x64 computer.

DEPENDENCY REQUIREMENTS ... 
This application requires Debian Linux, Apache 2.4, PHP 8+, FFMPEG, Dia, LibreOffice, 
Mkisofs, 7zip, Unoconv, libgxps-utils, Tesseract, Unzip, OpenSCAD, Rar, Inkscape, Calibre,
Unrar, ClamAV, MeshLab, PopplerUtils, PDFTOTEXT, ImageMagick, Assimp, Bwrap, Dia & xvfb-run.

<3 Open-Source
-----------------------------------------------------------------------------------


--------------------
COMMIT 8/23/2026
v3.8.0 - Polish CLI args --setup & --config. Fix bugs with apparmor, php.ini. Polish self-installer.

-v3.8.0.
-Fix time handling in listener logfiles.
  -The time reported in listener logs was confusing, because it was set once when the listener started.
  -Now we update timestamps every time the listener writes a log.
  -This is true for every logging tier.
-Fix apparmor config.
  -Use official documented include process instead of winging it.
  -We now include the default file correctly.
  -The -fp CLI arg now correctly fixes all versions of our apparmor config. 
  -If you have apparmor problems, run `sudo php convertCore.php -fp` and reboot the host.
-Fix bubblewrap config.
-Fix IM7 security profile.
-Add proper php-zip dependency manifest handling.
-Add php.ini configuration to --setup routines.
  -Use official documented include process instead of winging it.
-All of this is fixed by running the --fix-permissions command   `sudo php convertCore.php -fp` 
-Update docs.
  -INSTALLATION_INSTRUCTIONS.txt, ABOUT_COMMAND_LINE.txt, ABOUT_COMPONENTS.txt, README.md are all updated or new.
-Update dockerfile, entrypoint.sh.
-Add diagnosis script for bubblewrap.
  -Located in `Documentation/Build/diagnose-bwap.sh`.
  -Run with  `sudo bask diagnose-bwrap.sh`.
-Fix MeshLab / PyMeshlab detection in -v CLI output and Dependency related --setup commands.
-Make the -v CLI argument even more powerful & capable.
-Make the --fix-permissions  &  -fp  CLI arguments more powerful.
-Fix PyMeshLab supply chain audit output in generated `Supply_Chain.xlsx` files.
-Fix HRConvert2 Listener service handling.
  -Now installed by -fp command.
-To stop an old listener...
  -sudo php convertCore.php --kill
-To install the new listener as a service which runs at system startup (managed via systemctl command)...
  -sudo php convertCore.php --fix-permissions
-Now the listener runs at startup and can be controlled like any other service with systemctl commands.
-The docker install process is now a little different.
  -All the install dependency logic has moved into the core.
  -Dependencies are fully managed. You can install, update, reinstall, remove all from CLI.
  -You can do all that on a per dependency basis, or a per subsystem basis.
  -We leverage the self-install feature to simplify the dockerfile build process.
  -Also, this makes installing on VM or bare metal very easy.
-The new install process is 2 commands.....
  git clone https://github.com/zelon88/HRConvert2
  sudo bash HRConvert2/Documentation/Build/hrconvert2-setup.sh
-Update Default convertGui2.php UI file with some improved AJAX calls.
  -This will get improved even more, but it's better than what we had.
  -Once it's perfect it will get rolled into HRC2-Functions.js.
  -Previously on-error, the UI would redirect a user to a 404 page.
  -We fixed that before, but we had a regression due to some architectural changes.
  -This ajax should solve the problem once and for all, and it will get better.
-COMING SOON in a future version; iframes with self-reloading content. No more refresh button!

--------------------
COMMIT 8/23/2026
v3.7.9 - Add one command install. CLI install, configure, & dependency management arguments.

-v3.7.9.
-Administrators with root CLI access can now.....
  -Install, uninstall, update, & repair the application via CLI --setup commands (and child commands).
  -Install, uninstall, update, check status of ALL dependencies via CLI --setup commands (and child commands).
  -Output a supply chain audit of EVERY dependency.
  -Mange dependencies by subsystem.
  -Modify config.php via CLI arguments, completely without a text editor.
-The installtion & configuration of HRConvert2 is now fully done via CLI argument to commandCore.php.
  -See --config  and  --setup commands for more information.
-The -v argument is even more powerful.
-Formalized 'component' architecture.
  -Components are parts of HRConvert2 that are detachable, or may fail without taking down the entire application.
  -Components include, GUIs, Language Packs, config.php, dependencyCore, setupCore, coreManager.
  -The main 'Component' of HRConvert2 that cannot be replaced is convertCore.php. And always will be.
  -convertCore.php must be fully executable on it's own. Even if it has no components to operate with.
  -By breaking the application into well defined, well structured 'components' we increase quality, reliability, security, scalability, and auditability.
-Dependencies are now managed by a component called dependencyCore.php.
  -Managed as a version controlled component. Like a GUI, Language Pack, config.php, ect...
  -Logic moved from convertCore and coreManager.php, consolidated in dependencyCore.php.
-Dependency manifest is stored in Resources/depends.php.
  -This is also a version controlled component.
-Installation and configuration is now managed by a component called setupCore.php.
  -Managed as a version controlled component. Like a GUI, Language Pack, config.php, ect...
-Improve listener startup routine, move main logic to enable functioning as a linux service.
  -Add --run-core-manager command for starting the listener as a foreground service for this purpose.
  -When starting a listener manually via CLI, continue using -l or --listen.
-Strengthened $CoreLoaded logic. Added it to config, depends, and other components as a structural piece of a valid component.
  -This logic prevents any other logic from being executed when the main convertCore.php file is not the entrypoint.
-I am aware of a bug that affects bundled PyMeshLab detection. 
  -The dependency check will currently report 1 warning for PyMeshLab and claim that it is absent, even though it is present.
  -This is the only dependency that currently does not get reported correctly.
  -I am aware of the issue and it will be fixed very soon (before this gets pushed to docker).
-I am planning on doing a quality pass on this commit and having it up on DockerHub by the end of this week as v3.8.0.
  -Fingers crossed! Wish me luck, or venmo me and I'll work even faster! ;)  @zelon88 

--------------------
COMMIT 8/20/2026
v3.7.8 - Add scalable per-conversion CPU/RAM limits, rate controlled by server pressure.

-v3.7.8.
-Credit goes out to Dickson Massawe (Github user @archnexus707) for reporting a Security Advisory!
  -https://github.com/archnexus707
  -SSRF via unsandboxed LibreOffice document conversion (network isolation missing on the document path) 
  -https://github.com/zelon88/HRConvert2/security/advisories/GHSA-4jp5-8jhf-cmgw
  -GHSA-4jp5-8jhf-cmgw
  -His POC directly contributed to the LibreOffice / Document Pipeline improvements in v3.7.7.
  -Improvements include; 
    -Retiring Unoconv.
    -Sandboxxing document conversions with bwrap.
    -Creating custom tooling for sandbox validation.
      -Located at `Documentation/Build/bisect.sh`.
      -This file can be modifiied to test a target dependency for Bubblewrap sandbox compatibility.
      -The `bisect.sh` file will run a sample conversion using a supplied dependency in a variety of different sandboxes.
      -The test runs sandboxxes that are very permissive to very aggressive, and reports what the dependency will accept.
      -The goal of this tooling is to determine the LEAST PERMISSIVE environment that our dependencies will accept.
  -Thanks for making HRConvert2 better for everyone!
-Work on scalable per-conversion resouce limits.
  -These are scalable according to server pressure.
  -A server under low pressure provides more resources to the few conversions it gets.
  -A very busy server provides less per-conversion resources, to protect overall resource availability.
-Add documentation.
  -ABOUT_RESOURCE_RATE_LIMITING  .txt & .md.
-Added apparmor check to core listener.
-Added imagemagick policy check to convert core.
-Added apparmor & imagemagick policy repair routine.
-Found and fixed mutliple bugs, logEntry, errorEntry, warningEntry being called before they are available.
-Added quickDie() function to solve this and proceduralize the process of erroring before the logging environment is ready.
  -This will hopefully prevent this kind of bug from happening again.
  -Now I guess we have 4 tiers of logging. With the 4th being graceful shutdown without any logging at all.
-Update README.md, ABOUT_COMMAND_LINE.md, ABOUT_COMMAND_LINE.txt, & ERROR_DESCRIPTIONS.txt.


--------------------
COMMIT 8/20/2026
v3.7.7 - Functioanl rate limiting. Sandbox Document Conversions. Remove Unoconv. First functional resource listener!

-v3.7.7.
  -The good stuff first!
-----
  -To enable resource awareness...
  
  -Set `$AllowResourceAwareness` to `TRUE` in `config.php`.
  -Review and set other `$Resource` related `config.php` settings.
  -In a command line, start the listener with...
    `sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php --listen`
  -Check listener status with...
    `sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php --status`
  -Kill the listener with...
    `sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php --kill`
  -Review the help documentation!
    `sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php --help`
  -Perform a copmprehensive diagnostic & status report for each included component & all managed dependencies.
    `sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php --version`
  -Some commands require `sudo`, some don't. Some commands give different output as `sudo`. 
  -Commands are context aware, so they tell you when sudo is required.
  -If you plough sudo into everything (I see you. Heathen) it will safely reduce it's own context to www-data.

-----
-Ok, now the boring stuff.
-Rename the secret.php file with `$ApplicationName` from config.php.
  -We also version pin it and replace it when it gets outdated.
  -This ensures bugs that slip may into it are repairable from a programatic standpoint.
  -And that it's always valid to the current version of the core.
  -This can be manually checked by running `sudo php convertCore.php --status`.
-Added an EXTREMELY helpful `-fp` or `--fix-permissions` CLI argument.
  -When run as root, repairs the installation & all managed folders automatically.
  -`sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php --fix-permissions`
-Sandbox document conversions in the same way that all other conversion pipelines are sandboxxed.
  -Add a document rectifier that checks for and removes external references from docs that run through the document converter.
  -This was reported by a user and is being processed under a pending security advisory.
  -The security advisory will be published shortly.
  -Use bubblewrap with no network and specified namespaces.
  -Use only the least permissive settings we can achieve.
  -I will attach the tooling I used to determine the least permissive sandbox LibreOffice will accept in a comment under the security advisory.
  -Previously the Unoconv Listener + Soffice Listener prevented us from sandboxing document conversions like all the other conversion pipelines.
  -We now send commands directly to libreoffice, and (currently) absorb the cold-cache startup cost when applicable.
  -This means we can bwrap libreoffice with no-network and smaller namespace exposure.
-Remove Unoconv as a dependency.
  -This is huge.
  -Official Unoconv Github repo... https://github.com/unoconv/unoconv
  -Unoconv was of the oldest dependencies in HRConvert2.
  -It was introduced into the (then proprietary) "HRConvert" (no "2") in 2013.
  -Unoconv was supersceded by "Unoserver" by the same developer.
  -Unoconv has been deprecated and unmaintained since Mar 31, 2025.
  -Earier this year I (@zelon88) forked Unoconv and tweaked it to run with modern LibreOffice.
    -Specifically so we could keep using it.
    -I bundled UnoConv into HRConvert2 and shipped our modified version as our own.
    -The option existed for a time in config.php to pick which unoconv to use.
      -apt-get unoconv gives you a version that does not work with apt-get libreoffice
    -When we introduced dependency validation, $PathToUnoconv was removed from config.php & decided by the core at runtime.
  -Now we send our commands straight to LibreOffice instead. Directly.
  --PROS--
    -We can shut down the persistent listeners that used to run all the time.
    -We remove Unoconv as a dependency and no longer need to bundle it.
    -We get to run sandboxing directly on LibreOffice with no-network & sane amounts of namespace.
    -We can remove the rc.local setup COMPLETELY fom the `INSTALLATION_INSTRUCTIONS.txt` & `Dockerfile` builds.
  --CONS--
    -We (currently) do not have LibreOffice cache warming.
    -Document conversions are take considerably longer to complete.
    -LibreOffice must start with a fresh cache for every session.
    -Expect to observe considerable amounts of disk IO for every document conversion. Even when converting small files.
    -The cache creation cost is static. There's no avoiding it. a 1kb conversion means LibreOffice builds a fresh `.cache` and `.config` for every conversion.
  -I plan on integrating the libreoffice cache warming process into the `coreManager` somehow.
    -This is a secondary priority to getting the `coreManager` to actually run consistently and be stable.
    -The official "birthday" for `coreManager` is today. This commit marks the first time it actually works.
    -Please be paitient with it.
-ALL FOUR `coreManager` LISTENERS RUN!!!!
  -Resource awareness is live.
  -Four processes, started by one command. `convertCore.php -l` (root or www-data only).
    -Derives a time-bucketed HMAC startup key from the install secret and shell_execs `convertCore.php --start-core-manager <key>`.
    -Dropping to `www-data` via `su` if you ran it as root.
    -That process validates the key, becomes Core Manager, then spawns three subordinates the same way: 
      -Request Manager, 
      -Resource Manager, 
      -or Worker Manager.
    -Each `Manager` a full `convertCore` boot that enters its `Manager` role and never returns. Into the abyss...
    -Think of `convertCore` as the mothership. Or the queen of an ant colony.
    -Every `Manager` and every `Worker` starts as a clone of `convertCore`. 
  -Uses UDS Sockets for communication.
    -Unix domain sockets in `$ConvertLoc/Sockets`, `0700`, owned by `www-data`. 
    -Every message is AES-256-GCM encrypted with a key derived from the install secret per channel, carrying a timestamp and nonce.
    -Anything outside the skew window is discarded as a replay.
  -Request flow, ONE-WAY ONLY.
    -A worker can address exactly one socket — Request Manager's.
    -Request Manager sends budget, release, or extend; anything else is refused as a request type a worker may not issue.
    -Request Manager forwards to Core Manager, which routes by type: 
    -`budget/`release`/`extend` to Resource Manager, 
    -`kill`/`reap` to Worker Manager.
    -The verdict returns back down the same open connection.
    -No worker is addressable by anything, so there is no path from one worker to another.
  -The Resource Budget.
    -Resource Manager owns the resource budget and nothing else adjusts it.
    -Every $ResourcePollInterval it reads load average and available memory, then recomputes:
      -a base of $TotalResourceBudget (or 100 units per core when zero),
      -minus a reserve percentage,
      -minus a penalty scaled to whichever of load or memory is under more pressure.
    -An idle server throttles nothing; a loaded one stops accepting before it falls over.
    -An approved request registers the worker with its cost and expected runtime and returns a budget token, which the worker hands back on completion.
  -Reaping.
    -Worker Manager sweeps every $WorkerReapInterval.
    -A tracked worker whose process has exited, or which has outrun ExpectedRuntime + $WorkerStaleGracePeriod, is killed and its budget reclaimed.
    -It is the only component permitted to end a process without negotiation.
    -Core Manager separately restarts any subordinate it finds dead.
  -Failure is open by design.
    -No component, a version mismatch, a dead listener, an unreachable socket, or a delivered request with no usable reply — all of these approve the conversion and warn.
    -The core then behaves exactly as it did before any of this existed.
    -$RequireResourceAwareness = TRUE inverts that for operators who'd rather refuse than run unaccounted.
  -Stopping.
    -The `-k` argument asks Core Manager to stop, waits for a clean unwind, then signals what remains and verifies via /proc/<pid>/stat
    -Checks process state rather than directory existence, so a zombie isn't mistaken for a survivor.
    -The `-k <id>` argument ends one worker;
    -The `--kill-all-workers` argument ends every tracked one;
    -The `--kill-every-worker` argument ends every PHP process owned by www-data, including other applications on the host.
  -State that crosses processes:
    -manager PIDs,
    -the worker registry,
    -the current budget — lives in JSON files beside the sockets.
    -That's the weakest part: last-write-wins, so a busy server could drop a registration between read and write.
      -We will eventually implement an in-memory cache for bookeeping this to remove the reliance on the filesystem.

--------------------
COMMIT 8/19/2026
v3.7.6 - Repair the memory purge & secret obfuscation routines added earlier in v3.7.5.

-v3.7.6.
-Work on coreManager, listeners, resource management.
  -Just trying to get the listener structured, prototyped, and running.
  -The bones are there, but the listener won't start. Detection in convertCore.php is faulty.
  -What DOES work is fallback. If resource awareness is enabled, and the listener is broken, convertCore still converts files.
  -Need to update ERROR_DESCRIPTIONS.
  -Need to update -v output text.
  -Need to update -h output text.
  -Need to update ABOUT_COMMAND_LINE.txt doc.
-Repair the memory purge & secret obfuscation routines added earlier in v3.7.5.
  -addSillyString no longer emits bytes outside a PHP tag.
    -The previous payload placed newlines before <?php & between ?> and <?php.
    -Those bytes were printed on every require() of secret.php, before any header() call.
    -This corrupted AJAX responses & broke header delivery, the same way a BOM does.
    -The payload is now a single unclosed PHP region with an internal comment fence.
    -There is deliberately no closing ?> tag, per standard practice for include-only files.
    -Secret files already in the wild are still valid. They just have the old spacing.
  -purgeSensitiveMemory now throws a real error instead of an oversized warning.
    -The fatal branch called warningEntry with 3 arguments. warningEntry accepts 1.
    -PHP discards extra arguments to userland functions, so 40000 & the die flag evaporated.
    -ERROR!!! 40000 could never fire & the fatal path never halted anything.
    -That branch now calls errorEntry as the logging tier convention requires.
  -purgeSensitiveMemory no longer reports an already empty variable as a leak.
    -NULL matched no type branch, so a NULL target fell through to the failure path.
    -With the fatal branch repaired this would have killed every first run install.
  -purgeSensitiveMemory falls back before it errors, per the fallback convention.
    -An unhandled type now has its reference dropped & warns, rather than erroring outright.
    -ERROR!!! 40000 now fires only when that fallback has also failed.
  -purgeSensitiveMemory names the correct culprit from inside a recursive call.
    -An inner call resolved getChildFunction to purgeSensitiveMemory itself.
    -The caller name is now resolved once, on the outermost call, & carried down.
    -This also stops one debug_backtrace call per array element.
  -purgeSensitiveMemory logs the behaviour it was actually given.
    -The message read from the global while the decision came from the argument.
    -It now reports the argument & says so when the two disagree.
  -purgeSensitiveMemory can report before verifyLogs has run.
    -It is called by installation code that runs before the logfile exists.
    -It now falls back to the server error log, or to die, when $LogFile is not usable.
  -purgeSensitiveMemory returns $MemoryIsPurged, TRUE on success, per convention 8.
  -Every caller now passes targets DIRECTLY BY REFERENCE instead of inside an array.
    -array($SecretKey, ...) copied the values. The function shredded the copies.
    -Every original buffer survived the call fully intact. The feature did nothing.
  -resolveSecretFile initializes $SecretKey before the require that defines it.
    -An install that failed to create a file handed an undefined variable to the cleanup.
  -Remove the unused $secretSillyString variable from resolveSecretFile.
  -Correct the purgeSensitiveMemory docblock. Argument order & fatal polarity were both inverted.
  -Document ERROR!!! 40000 in ERROR_DESCRIPTIONS.txt.

--------------------
COMMIT 8/19/2026
v3.7.5 - Start work on concurrent rate planning.

-v3.7.5.
-Improve memory cleanup routine.
  -Take the time to truly understand how memory structures work inside PHP, at the deepest possible level.
  -Space for clarity.
  -Space for clarity.
  -Space for clarity.
  -Space for clarity.
  -It turns out our understanding was incomplete, and the devil is in the details.
  -Our approach until now has revolved almost entirely about 'reducing the temporal window of availability of data on the heap'.
  -This is a good approach, and it is valid to want to reduce the temporal window of data availability on the heap.
  -What our past approach lacked due consideration for is the *Footprint* of data availability.
  -By not fully understanding the *Footprint significance, we made inaccurate assumptions regarding the *Scope* of data availability.
  -PHP is a messy beast. It allocates memory simply on *Reference to a variable. So you get the same variable in memory numerous times.
  -The variable copies are not accessible easily via reference. The process for clobbering them is manual, and must be deliberate.
  -Destroying the original variable DOES NOT destroy the copies. It only destroys the original.
  -So simply the ACT of trying to destroy a variable can actually CREATE new instances of the data that variable contains in memory.
  -In order to effectively "Protect" memory, we must manage PHP memory allocations that PHP does expose to us by default.
  -We must consider the footprint we are creating in memory when we are merely *REFERENCING variables.
  -We must then make the *BEST DECISION regarding when, how, or why to REFERENCE a variable only after considering it's temporal window of exposure and the size of it's exposure footprint.
  -A good example of this is string concantenation. Every time you re-define a variable that is already defined, a new copy of that variable is stored in memory.
  -So using '.=' to update a variable "in-place" actually creates A NEW COPY of that variable in memory, and updates your ONLY reference to point to the newest copy.
  -The old copy and the new copy now exist in memory at the same time.
  -We cannot recover or clean the old copy after we PHP updates the reference to $variable. 
  -We can only ever purge whatever $variable points to in memory at that time.
  -All past copies of $variable are are located at memory addresses we do not know, and PHP does not expose to us.
  -Therefore, the best way to "protect" variables requiring concantenation is TO NOT CONCANTANATE THEM.
  -Instead, declare NEW TEMPORARY VARIABLES and destroy those when no longer needed.
  -It seems counter-intuitive that declaring MORE variables actually results in LESS memory footprint. But it's true.
  -The reason it's true is.... New variables can be recovered for destruction. Re-written ones cannot.
  -So it is CLEANER to declare MANY variables with narrow scope and destroy them than it is to redeclare the same variables over and over again.
  -For all intents and purposes, LOWER CASE FIRST LETTER VARIABLES MUST BE ONE-TIME-USE DISPOSABLE!!!!!!!!
  -Update CODING_CONVENTIONS.txt & .md to explicitly state not to redeclare narrow scope variables do to PHP Copy on Write (COW) variable architecture.
  -Space for clarity.
  -Space for clarity.
  -Space for clarity.
  -Space for clarity.
  -Update `ABOUT_DEFENSIVE_MEMORY_MANAGEMENT.md` to match what is actually built.
  -Functionalize memory cleanup to improve safety & ease of use.
  -Turns "WET" code (2 cleanup lines per function) into a clean function call + result check.
  -Memory protections are now enforced or not enforced, optionaly via config.php.
  -Provides a tiny performance boost at the expense of additional temporal memory exposure.
  -This is now in a purge function.
  -Memory is protected regardless, config.php setting $EnforceMemoryProtections just decides what to do when memory protection fails.
  -If $EnforceMemoryProtection is enabled in config.php, the application will throw a fatal error if memory cannot be protected.
  -If $EnforceMemoryProtection is enabled in config.php, the application will throw a warning and continue if memory cannot be protected.
  -Added ERROR!!!, 40000 - 'Cannot purge sensitive memory for the '.$FunctionName.' function! Memory protection is enabled in config.php. Execution cannot continue!'
  -Added WARNING!!! - 'Cannot purge sensitive memory for the '.$FunctionName.' function! Memory protection is disabled in config.php. Continuing...'
  -These warnings both print the calling function name in the log.
  -Add getChildFunction() function to get the name of a calling function for this log entry.
  -Note the purge memory routine does not purge it's own memory, that would be an infinite loop generator instead.
    -So we have to be careful about initializing variables in this function, as they don't get protected themselves.
-Add decoy $SecretFile contents loop.
  -This drops decoy lines into the secret file, as a tar pitt for amatures or low-effort LLMs grepping for secrets.
  -This does not effect $SecretFiles that already exist.
  -Existing $SecretFiles in the wild are still valid. They just dont have decoy lines.
  -Maybe eventually we will need version control, but not yet.
-Add or update functions... getChildFunction, resolveSecretFile, addSillyString, purgeSensitiveMemory, getChildFunction, verifyContainerEnvironment.
  -purgeSensitiveMemory takes as a second input argument a boolean that determines if the function throws a non-fatal warning or a fatal error on failure.
  -In any code path, a failure writes a log message containing the name of the calling function.
  -The name of the calling function is important, because otherwise a troubleshooter would look at purgeSensitiveMemory as the culprit, when it is probably just the messenger.
  -purgeSensitiveMemory takes the fatal argument instead of using $EnableMemoryProtection as global so it can be used before verifyInstallation runs.
    -Note that this does not "free" destroyed memory. The memory values are overwrittein in place with 0.
    -"Destroyed" memory allocations still consume the same amount of memory. The memory value is just replaced with 0.
    -Early running calling code just needs to manually decide if it wants the error to be fatal or not.
    -Late running calling code can simply pass $EnableMemoryProtection to purgeSensitiveMemory as the second argument.
    -purgeSensitiveMemory reports the calling function name as the culprit when it writes any log. So calling code properly shares blame.
    -Otherwise, this would kill the application and throw ERROR!!! 40000 and it would be a mystery which invocation of purgeSensitiveMemory was the cause.
-Add config.php variables... $EnableMemoryProtection
  -Add to verifyInstallation globals line, verifyConfigVersion required variables array. 
  -Bump config version to v3.7.5.
-Working on `coreManager.php` listener components & `convertCore.php` worker+listener components.
  -There are basically 3 daemons rolled into convertCore.php & coreManager.php, all called through CLI invocation to convertCore.php.
  -Work on cryptographic keying structure.
    -Understand features & limitations of the design.
  -Add  ERROR!!! 50000, FATAL, in convertCore.php.
    -'Could not format a resource request! '.$RequestType.' request.'
  -Add  ERROR!!! 50001, FATAL, in coreManager.php.
    -'The resource planner daemon (coreManager.php) is not running, but is configured to be required. This operation cannot continue!'
  -Add  ERROR!!! 50002, FATAL, in coreManager.php.
    -'Coud not enumerate a required device! Please verify that '.$addedConvertLoc.' is valid!'


--------------------
COMMIT 8/16/2026
v3.7.4 - Add support for EBook files & additional image formats.

-v3.7.4.
  -Bump config.php, all 3 GUIs, and all 26 language packs to v3.7.4.
-Add support for Ebook documents using calibre.
  -Install with `sudo apt-get install calibre`
  -We may revise the version we're using soon, when we introduce supply chain auditing.
  -Input EBook support:
    -'epub', 'mobi', 'azw3', 'fb2', 'lit', 'lrf', 'pdb', 'pml', 'rb', 'snb', 'tcr', 'txt', 'txtz', 'html', 'htmlz', 'rtf', 'oeb', 'docx'
  -Output EBook support:
    -'epub', 'mobi', 'azw', 'azw3', 'azw4', 'fb2', 'fbz', 'lit', 'lrf', 'pdb', 'pml', 'rb', 'snb', 'tcr', 'txt', 'txtz', 'html', 'htmlz', 'rtf', 'odt', 'docx', 'pdf', 'chm', 'cbz', 'cbr', 'cbc', 'prc', 'opf', 'recipe'
-Add support for 6 additional image formats.
  -If on bare-metal / VM, this requires rebuilding IM7 from source using the provided script.
  -The script is located in `Documentation/Build/build-imagemagick-v7.sh`
  -Docker users can just download a new docker image.
  -tiff
  -tif
  -heif
  -jp2
  -j2k
  -jxl
-Update ImageMagick7 build script.
  -The script is located in `Documentation/Build/build-imagemagick-v7.sh`
  -It now installs dependencies.
  -It now cleans up the build location, so you can run it twice in a row.
  -It now reinstalls the locally installed source built IM7 if it exists.
  -It now builds a more capable version of IM7 that has more reliable support for more formats.
-Update README.md to.....
  -Describe new bootable format support.
  -Increase conversion count.
  -Add screenshots,
  -Revise some sections.
-Update documentation.
  -Add ABOUT_REST_API.txt.
  -Add ABOUT_REST_API.md
  -Update CREATING_GUIS.txt (which technically has always held the REST API information).
  -Updated README.md on Dockerhub.
    -https://hub.docker.com/repository/docker/zelon88/hrconvert2/general
  -Added CREATING_CONVERSION_PIPELINES.txt.

--------------------
COMMIT 8/16/2026
v3.7.3 - Add support for bootable ISO images. Fix Default UI Generate Share Link.

-v3.7.3.
-The Generate Share Link button was broken in the Default UI.
  -Reported by zelon88 (me) in  https://github.com/zelon88/HRConvert2/issues/104
  -The reason was copypasta in HRC2-Functions.js that repalced the function required to generate the link.
-Add support for bootable iso images.
  -Supports MBR, GPT, MBR+GPT, GPTx86, GPTx86-64, and latest GPT-ARM32 and GPT-ARM64.
  -Converts any archive format into bootable iso.
  -The bootable iso pipeline verifies dependencies, utilized pre-baked binaries, and hashes it's binaries at runtime to ensure they are valid.
  -Implement dependency version pinning & checking.
  -Implement bubblewrap namespaces for bootlable iso operations.
  -Add documentation about how bootable iso creation works, and about how to generate the binaries when it's time to update them.

--------------------
COMMIT 8/16/2026
v3.7.2 - Start working on dependency path validation. Improve non-sudo CLI operation.

-v3.7.2.
-Add a bunch of .md documents.
  -.txt documentation files are for engineering intent.
  -.md documentation files are for human consumption, like a user manual for a hobby product you enjoy reading.
-Add dependency checking to images as a test.
  -Leaving streams & LibreOffice unsandboxxed FOR NOW.
    -The stream SSRF protector is the security boundary.
    -If we wrap with bubblewrap, the PID detection routine will grab PID from bwrap, not ffmpeg.
    -To reliably track streams, we must leave the PID and bwrap out of the picture, only track ffmpeg.
    -We will add worker lifespan tracking during the upcoming "Concurrency Rate Limiting" update.
    -After worker lifespan cycle tracking is complete,the core will be structurally prepared for sandboxxing convertStreams() safely.
  -Add ffmpeg, inkscape, 
  -OpenSCAD adjust rectifySCAD to use relative paths instead of absolute ones.
    -Bubblewrap namespace sandboxxing requires this.
    -Also adjust the return value to capture if any includes were abandoned by OpenSCAD during the render.
    -If any includes were skipped by OpenSCAD, throw a warningEntry() with that information to the log.
  -Fix missing convertSubtitles() version check error message, ERROR!!! 22002.
  -Added a ton og new errors.
-Next we are going to work on capability validation & concurrency rate limiting.

--------------------
COMMIT 8/16/2026
v3.7.1 - Start working on dependency path validation. Improve non-sudo CLI operation.

-v3.7.1.
-Add dependency checking throughout.
-Add userland secret & log creation.
  -Now command line arguments work properly without 'sudo'.
  -Only -v and -h commands work without sudo.
  -The -c and -u commands still require sudo, as these require modifying the installation or data directories.

--------------------
COMMIT 8/16/2026
v3.7.1 - Start working on dependency path validation. Improve non-sudo CLI operation.

-v3.7.1.
-Add dependency checking to images as a test.
  -Leaving streams unsandboxxed FOR NOW.
    -The stream SSRF protector is the security boundary.
    -If we wrap with bubblewrap, the PID detection routine will grab PID from bwrap, not ffmpeg.
    -To reliably track streams, we must leave the PID and bwrap out of the picture, only track ffmpeg.
    -We will add worker lifespan tracking during the upcoming "Concurrency Rate Limiting" update.
    -After worker lifespan cycle tracking is complete,the core will be structurally prepared for sandboxxing convertStreams() safely.
  -Add ffmpeg,
  -Fix missing convertSubtitles() version check error message, ERROR!!! 22002.
-Improve non-sudo CLI operation.
  -Add userland secret & log creation.
  -Add gates to lock down the arguments from even being considered unless the correct security context exists.
  -Now command line arguments work properly without 'sudo'.
  -Only -v and -h commands work without sudo.
  -The -c and -u commands still require sudo, as these require modifying the installation or data directories.

--------------------
COMMIT 8/15/2026
v3.7.0 - Add auto update via CLI. Add root detection. Improve core logic ordering.

-v3.7.0.
-Update CREATING_GUIS.txt, CREATING_LANGUAGE_PACKS.txt, DOCKER_BUILD_INSTRUCTIONS.txt.
-Separate the detection logic for Meshlab & Assimp so that the verifyModelVersions() function detects both separately.
-Update showHelpInfo with latest description of supported command line args, and description of update functionality.
-Add -c, --clean argument which accepts =## or =now.
  -This will clean the ConvertLoc and the ConvertTempDir immediately using the supplied ## as a $DeleteThreshold.
  -If -c or --clean is supplied with no optional Delete Threshold, the default from config.php will be used instead.
-Update cleanDataLoc() function to simplify the authorization checks that take place before any cleaning operation is approved.
  -The function now accepts $deleteThreshold as a third argument, instead of calling $DeleteThreshold by global reference.
-Add USING_COMMAND_LINE.txt which contains information to teach administrators proper command line usage.
-Implementing new Documentation structure using .txt files as engineering documents, sources of truth, and .md files as human readable.
-Implement bubblewrap namespaces accross all major dependencies.
-Implement escapeshellargs across all major dependency calls.
-Fix convertImages() -alpha remove ordering bug predates the sandbox. Any transparent PNG converting to JPEG was failing before today.
-Fix Dia having been removed accidentally from header files.
-Fix bugs in ocrFiles, update ImageMagick code path from IM6 to IM7 code format.
-Add $RequireSandbox variable to config.php.
  -We perform a comprehensive check to see if bwrap is needed, or possible.
  -Bwrap is not needed on docker, because the container is... a container.
  -Bwrap is also not POSSIBLE on docker. So don't let it run on Docker.
  -Bwrap is only enforce if $RequireSandbox is true, and bwrap is determined to be available on the system.
-Update convertArchives to fix bugs.
-Update ocrFiles to fix bugs.
-Add $RequireSandboxOnDocker to config.php.
-Add $ThrowSandboxWarning to config.php.
-Add version checking for convertArchives.
-Update dockerfile to include installation for p7zip-rar which was missing.
-Added too many errors to count.
  -Fifteen entries: 8002, 9002, 9003, 10001, 11002, 12002, 13006, 13007, 13008, 13009, 13010, 13011, 13012, 13013, 15012, 22001, 25002.
  -We will need to go through ERROR_DESCRIPTIONS.txt and thin it out at some point.

--------------------
COMMIT 8/15/2026
v3.6.9 - Add auto update via CLI. Add root detection. Improve core logic ordering.

-v3.6.9.
-Add http:// & https:// filtering to sanitize as the first layer.
  -This will cleanly remove URLs, in preparation for accepting them as input for stream operations where the URL is missing.
  -Currently URLs still get sanitized and do not pose a threat because : and / are removed.
  -This just adds a dedicated layer which removes both http:// and http:// in one pass (each) so the 'http' doesn't contaminate the URL.
-Update -u & --update arguments to accept version from command line, like -u=edge, or -u=latest, or -u=v3.6.9, or --update=edge, or --update=latest, or --update=v3.6.9
-Update cleanDataLoc() function to only authorize a clean operation if it is being targetted against the $ConvertLoc or the $ConvertTempDir.
  -The function will no longer even consider firing the at any other location, or a location that does not match what the calling code specifically intended.
  -This will hopefully prevent people who are working on the code from clobbering their own installation with the cleanDataLoc() function by accident. It happens.
  -Add ERROR!!! 29 - 'An invalid clean operation has been blocked!''
  -Adjust the text of the log from "Deep Cleaned: " to "Files Removed: " which is more accurate, descriptive & straightforward.
  -Move the log entry to happen only if the $dataLoc even exists.
    -Previously it would fire any time the function was called, even if no file was cleaned.
-Add GUI + Language Pack version to -v and --version CLI output.
-Clean up early return, code quality of convertImages() function.
-Clean up early return, code quality of convertModels() function.
  -Separate the assimp & meshlab version detection logic.
  -Also separate the version reporting when -v or --verbose CLI arguments are used.
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29000, <SESSION_IDENTIFIER>: Could not determine the latest release from the update source!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29001, <SESSION_IDENTIFIER>: The requested update target is not a recognized version!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29002, <SESSION_IDENTIFIER>: The requested update version does not exist at the update source!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29003, <SESSION_IDENTIFIER>: The update package is larger than the configured maximum!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29004, <SESSION_IDENTIFIER>: Could not download the update package! Curl exited with code <ADDITIONAL_DATA>.
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29005, <SESSION_IDENTIFIER>: The downloaded update package is empty or too small to be valid!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29006, <SESSION_IDENTIFIER>: Could not read a configuration file during the merge!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29007, <SESSION_IDENTIFIER>: Could not merge the configuration file!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29008, <SESSION_IDENTIFIER>: Could not write the merged configuration file!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29009, <SESSION_IDENTIFIER>: Automatic updates are disabled in config.php!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29010, <SESSION_IDENTIFIER>: Could not create a working directory for the update!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29011, <SESSION_IDENTIFIER>: Could not extract the update package!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29012, <SESSION_IDENTIFIER>: The update package does not have the expected structure!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29013, <SESSION_IDENTIFIER>: The update package does not contain convertCore.php!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29014, <SESSION_IDENTIFIER>: Could not stage the update beside the installation!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29015, <SESSION_IDENTIFIER>: Could not move the existing installation aside!
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29016, <SESSION_IDENTIFIER>: Could not move the update into place! The previous installation was restored.
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29016, <SESSION_IDENTIFIER>: Could not move the update into place! The previous installation was restored.
-Add ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-29018, <SESSION_IDENTIFIER>: The updated installation failed validation & COULD NOT be rolled back!

--------------------
COMMIT 8/14/2026
v3.6.8 - Add auto update via CLI. Add root detection. Improve core logic ordering.

-v3.6.8.
-Add running as root detection / context awareness to verifyRequiredDirs() & verifyLogs().
  -This is required now that we expect convertCore.php to properly parse command line arguments.
-Update ImageMagick v7 build script to add a 5 try retry loop to the download to avoid failures at the Docker build stage.
-Improve verifyRequiredDirs().
  -The checks now enforce permissions that survive for web users even if other root or non-root users touch the application via CLI.
  -This improves usability & prevents confusing errors.
  -Also improved code comments to explain engineering intent behind some of the decisions that are being made.
-Reorder the core a bit. Clean up core logic section at the bottom to make it more legible and even.
-Add ERROR 27!!! - 'Could not verify user type!
  -This tries to see if the user is running via CLI or Apache+PHP.
-Add ERROR 28!!! - 'The application is being run in an unsupported security context!'
  -This checks to ensure that the script is not being used to run file operations as the root user.
-The checks behind errors 27 & 28 and are related in nature.
  -They protect against similar types of potential file corruption.
  -These checks enforce that the correct users are used, CLI is only for updates & diagnostics, and webUI is only for apache+PHP.
-Added auto updates.
  -Run the updater from a command line with   'sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php -u'
  -Check the versiona afterwards from a command line with   'sudo php /var/www/html/HRProprietary/HRConvert2/convertCore.php -v'
  -The -v or --version checks are extremely comprehensive. They validate every HRConvert2 component & every major dependency.
  -It is more of a diagnostic evaluation than a simple "echo version" like most other applications.
  -Admins with access to root CLI can now update the application.
  -The update process preserves existing config.php files, evaluates their compatibility, and tells you what changed.
  -The update process validates the versions of the components it installs.
  -The update process keeps a backup copy of the old installation for restoration or reference.
  -Default backup location is $ConvertDir/Last-Installed-Version.
  -The Last-Installed-Version folder is removed automatically after the $DeleteThreshold.
  -There are config.php entries for max update filesize, enable/disable auto updates, target repo, and more.
  -This should make maintaining a bare-metal or VM install of HRConvert2 much more practical.
  -Updates are performed by staging the new code in an offline location, and then swapping it in instantly.
  -Sessions are not broken, nobody gets 404 pages, and there is zero downtime.
    -Literally, a user spamming the refresh button would not even notice.
    -One page would load at v3.6.8, and the next would load at v3.6.9. 
    -Users in the middle of converting files during the update do not lose their files or sessions.

--------------------
COMMIT 8/14/2026
v3.6.7 - Fix typo in Vietnamese + Chinese. Add CLI arguments.

-v3.6.7.
-Update Chinese & Korean languageStrings.php files to fix typos (improperly escaped html tags in convertGui1.php).
  -Note: The version number at the header of the file (commented out seciton) changed to v3.6.7 to signal when this file was updated most recently.
  -Note: The version number contained in the version variable remains at it's original v3.6.4.
  -Note: The UI version also remains unchanged.
  -The reasoning behind updang the cannonical version number and not the logical one is that the logical structure of the file did not change.
    -No variable names were altered, the functionality of the file did not change.
    -We only fixed a typo.
    -Logical version numbers should only change if there is a technical reason why the language pack will not work with the UI anymore.
    -Typo changes, or grammer modifications to singlular language packs constitute a bump in cannonical version, but not logical version.
    -Logical versions of language packs MUST ALWAYS AND ONLY be updated all at once. All or nothing. All language packs must match, broken ones will not be used.
-verifySCADVersion() now accepts $MinimumSCADVersion by function call instead of as a global.
  -This is more consistent with the way the other dependency version verification checks are done.
-Add command line parsing.
  -Currently focusing on implementing comprehensive Dependency Version Reporting / Checking, Help text, and Automatic Application Updates.
  -I am NOT currently focusing on turning HRConvert2 into a CLI file converter.
    -File conversions ARE NOT currently being worked on for CLI deployment.
    -Only CLI version checking, diagnostics, and automatic updates are being developed currently.
  -parseCommandLine() parses the command line arguments that were supplied.
  -If any CLI arguments were supplied, the WebUI and all conversion logic is disabled.
  -The application will terminate immediately after parsing the requested command line operation.
  -File conversions ARE NOT supported via command line argument. Currently. Maybe someday.
-Add support for  -v  and  --version  command line (CLI) arguments.
  -Performs a comprehensive dependency check to ensure all version pinned dependency requirements are met.
  -Performs a comprehensive check on all bundled GUI packs, language packs, and config files.
-Add support for  -h  and  --help  command line (CLI) arguments.
  -Dispalys help information that would be useful to an administrator during debugging or troubleshooting.
  -Refers to specific files in the Documentation folder that could help with specific problems.
-Fix regression that removed bwrap from OpenSCAD conversions.
  -Add verifyBwrap() funtion to prevent this kind of thing from happening again.
-Fixed .github folder cleanup at runtime.
-Add permissions enforcement to required folder cleanup process.

--------------------
COMMIT 8/13/2026
v3.6.6 - Update Docker release process. Utilize Github Actions.

-v3.6.6.
-Improve Docker build process using Github Actions.
  -Now the Docker build updates automatically on Tagged releases.
  -This will enable me to use Tagged releases more often & more effectively.
  -Now, whenever a micro-project is complete and the core is stable; I can release a tag.
  -Before the cost of preparing the Docker was immense, and I would rather just do project code instead.
  -This takes the burden (and resource usage) of constantly building Docker images (which compiles FFMPEG & ImageMagick from source).
-Add the .github folder to the web root, but it is deleted on first run of HRC2.
  -The .github folder has the docker-build.yml file required for the Github Actions build process.
  -This folder is required for Github Actions to work.
  -The folder contains index.html files for default document root protection.
  -The folder is removed as part of the $RequiredCleanupFolders routine.
  -Note that this is not part of the "--Delete Development Environment--" setting.
  -Any user who wants to use Github Actions would need to completely regenerate this file anyway.
  -Therefore, it is not considered part of "The Build Environment."
  -It is a part of --MY-- build environment.

--------------------
COMMIT 8/11/2026
v3.6.5 - Add Multi-Stage Meshlab->Assimp Conversion Pipeline for 3D Models.

-v3.6.5.
-Expand 3D Model Support, reliability, consistency, & performance.
  -Add support for using PyMeshLab instead of MeshlabServer (which is deprecated & not coming back).
  -Instead of adding dependencies, I forked & bundled PyMeshLab, as it is GPLv3.
  -https://pypi.org/project/pymeshlab/
  -https://github.com/cnr-isti-vclab/PyMeshLab
  -https://github.com/cnr-isti-vclab/PyMeshLab/blob/main/LICENSE
  -Add verifyModelVersions() function for verifying meshlab & assimp.
    -Add ERROR!!! 9001, One of the 3D Model Converters version is missing, unidentifiable, or too old!
  -Use multi-stage conversion pipeline with Meshlab & Assimp.
  -The Route 2 Direct Intercept:
    -Formats like .fbx, .gltf, and .blend are routed directly to Assimp. 
    -This saves CPU cycles by bypassing MeshLab entirely.
    -Also prevents animation bone structures or nested scene materials from being accidentally stripped or flattened.
  -The Route 1 Double Stage Buffer: 
    -Formats like .stl, .ply are corrected via meshlabserver, converted to intermediate .obj format, & exported through Assimp.
  -An Anonymous Catch-All Block:
    -If a user uploads an unclassified 3D format, default to running a classic single-stage MeshLab execution directly to the final destination.
-Note that the config.php has been bumped to v3.6.5
  -Added min versions for imagemagick, meshlab, & assimp.
  -Added toggle for $UsePyMeshLab which tells HRConvert2 to use the bundled PyMeshLab for 3D model conversions instead of MeshlabServer.
  -Supported 3D formats have been expanded and now include..... 
    -'stl', 'ply', 'off', '3ds', 'obj', 'fbx', 'dae', 'gltf', 'glb', 'obj', '3mf', 'x3d', 'dxf' 
-Update CREATING_GUIS.txt & CREATING_LANGUAGE_PACKS.txt.
-Added missing pacman spinners (1 through 6) for orange, purple, & dark color schemes.
  -https://icons8.com/preloaders/en/circular#
  -Thanks, icons8.com!
-Improve convertImages() function to target jpgs for transperancy handling via alpha channel filtering.
  -Update from convert system call to calling /usr/bin/magick directly.
  -The function now requires imagemagick v7.
  -Add a build script for downloading, compiling, and installing imagemagick v7 in /tmp.
  -Add verifyImageVersion() function for ensuring imagemagick compatibility.
  -Add ERROR!!! 8001, The installed ImageMagick version is missing, unidentifiable, or too old!

--------------------
COMMIT 8/10/2026
v3.6.4 - Add version checking & fallback chains for GUIs & language packs. Finish Selctor Bar.

-v3.6.4.
-Add a UI selector bar to convertGui1.php & convertGui2.php in all three UIs.
  -Users can switch language, color & interface without editing a URL.
  -Collapsed behind a gear so it never displaces the upload dropzone.
  -Floats opposite $GUIAlignment, so it sits on the trailing edge in both reading directions.
  -Tokens are POSTed & never placed in a query string, because $SesHash derives from Token1.
  -A selector that dropped the tokens would discard the users uploaded files.
  -A selector that put them in a URL would leak them into history & access logs.
  -Each button overrides the form target with formaction & carries the two settings it is
   not changing at their current value.
  -showFiles & noGui are carried through so a selection returns to the page the user was on.
-Make $SupportedLanguages multidimensional.
  -The key is the ISO 639-1 code & the folder name. The value is the endonym.
  -The endonym is the name of the language in that language itself.
  -A user who has landed on a language they cannot read still recognizes their own.
  -verifyLanguage() now tests membership with array_key_exists() rather than in_array().
-Finalize flag icons in every GUI & every language pack.
  -Icons from https://github.com/lipis/flag-icons by lipis. Thanks again!
  -aii, arc & sw have no country & therefore no flag.
    -Assyrian & Aramaic have no state. Swahili spans Tanzania & Kenya with no single claim.
    -A national flag would also be a political statement in exactly the communities that
     motivated adding those languages in the first place.
    -Those three use a letterform instead. The first letter of the endonym, on a solid ground.
  -Flags were rendered from SVG to 24x18 PNG using HRConvert2's own new SVG converter.
-Improve GUI version checking to verifyGui().
  -uiVersionInfo.php carries $GuiVersion. The core carries $RequiredGuiVersion.
  -The check is an EXACT match, not a minimum.
    -A UI newer or older than the core expects may render incorrectly in either direction.
    -This differs from config.php, which is accepted at or above a minimum.
  -This exists mostly to catch my own mistakes during development.
    -Changing one GUI & forgetting the other two is easy & the result is hard to diagnose.
-Improve language pack version checking to buildGUI().
  -languageStrings.php carries $LanguageVersion.
  -The core carries $RequiredLanguageVersion.
  -The check lives in buildGUI() because that is where the pack is required.
  -The version is inside the file, so the file must be loaded before it can be read.
-Add fallback chains to verifyGui(), verifyLanguage(), verifyColors() & buildGUI().
  -A broken, missing or version incompatible GUI now falls back to the default GUI.
  -A broken or version incompatible language pack now falls back to the default pack.
  -An invalid color falls back to blue.
  -Every fallback writes a warning naming what failed & what replaced it.
  -Errors 15, 16, 17 & 25 now fire ONLY when the DEFAULT is also broken.
    -At that point nothing is left to render & they remain fatal, which is correct.
    -Previously they fired on any failure & said nothing about what was actually wrong.
  -verifyGui() builds a candidate list & loops it, so the path variables are built once.
  -buildGUI() loads the default pack OVER a bad one, because a pack is only assignments.
    -A UI file prints HTML & cannot be replaced the same way, which is why the GUI check
     happens in verifyGui() before anything is required.
-Fix $DefaultLanguage & $DefaultGui being trusted without validation.
  -Both were validated on the user-selectable path & assigned unchecked on the other.
  -An administrator typo plus selection disabled produced a directory that does not exist.
-Add $GuiSelectorText1 through $GuiSelectorText4 to all languageStrings.php files for the selector bar.
-Fix userScanCore operations erroneously always returning 'Infected'.
-Fix OCR multi-page jpg detection using stripos incorrectly.
-Review all use of strpos & stripos for accuracy.
-Switch from if ($Verbose) logEntry(); for infected files to warningEntry() which fires without $Verbose.

-NEED TO UPDATE ERROR_DESCRIPTIONS.txt for ERROR # 25.
-NEED TO UPDATE CREATING_LANGUAGE_PACKS.txt & CREATING_GUIS.txt.
-Need to condense / refactor CODING_CONVENTIONS.txt.

--------------------
COMMIT 8/10/2026
v3.6.3 - Update UI, add selector buttons, add UI & language pack compatibility checking.

-v3.6.3.
-Continue UI Selector bar work.
  -Make a gear icon to put the options behind.
-Use map icons from.....
  -https://github.com/lipis/flag-icons/blob/main/flags/4x3/it.svg
  -Thank you lipis!
  -Updated ICON_CREDITS.txt with Lipis' information.
-Map of languages to countries used.
  -en→us or gb   fr→fr   es→es   zh→cn   hi→in   ar→sa
  -ru→ru   uk→ua   bn→bd   de→de   ko→kr   it→it
  -pt→pt or br   vi→vn   tr→tr   ja→jp   id→id   pl→pl
  -nl→nl   sw→tz or ke   my→mm   ur→pk   fa→ir   he→il
-Add GUI version checking to the core.
  -The core now checks the version of the GUI for compatibility.
  -If an installed, configured, and selected GUI does not meed the minimum version requirements of the core, the GUI will not be loaded.
  -When the GUI cannot be loaded due to version discrepency, existing ERROR 25 is thrown.
  -Added the following lines to ERROR 25 description in ERROR_DESCRIPTIONS.txt.....
    -The version of the GUI that was loaded did not meet the Minimum Required Version that the core can accept.
    -An update did not complete successfully, or a recently installed GUI is corrupt or not compatible with the core.
  -This helps me keep track of UIs during development.
    -Sometimes I will make changes to one GUI, and forget to change the others.
    -After a GUI gets several commits behind it is hard to remember what that GUI needs done to it.
    -By having version enforcement, I am essentially saving myself from screwing the GUI up beyond recognition.
    -Bonus: If I DO screw up the GUI version levels, the GUI will tell me what the last version was.
    -This should also cut down on un-updated UIs showing PHP errors or corrupted page content when versions don't match.
  -Note that this update affects the verifyGui() procedure mostly.
  -Note that the version check is blind to actual version.
    -In other words, the version checks for a specific version ONLY.
    -The version check DOES NOT check for a range of versions that are acceptable.
    -This differs from config.php file checking, which is accepted if the version is greater than a set minimum.
    -With UI and LanguagePacks, there are forward / reverse compatibility concerns.
    -The core will specify exactly which version of UI is usable, as anything newer or older may not render correctly.
    -This functionality may change in the future.
    -Right now there's only one version of UI that would work anyway, as older versions lack the version variable required for version checking.
-Add language pack version checking to the core.
  -The version string goes into languageStrings.php.
  -The check logic goes into buildGui() as that is where languageString.php is loaded.
  -Update ERROR 17 in ERROR_DESCRIPTIONS.txt.....
    -The version of the language pack that was loaded did not meet the Minimum Required Version that the core can accept.
    -An update did not complete successfully, or a recently installed GUI or language pack is corrupt or not compatible with the core.
    -The application has become corrupt.
  -Note that the version check is blind to actual version.
    -In other words, the version checks for a specific version ONLY.
    -The version check DOES NOT check for a range of versions that are acceptable.
    -This differs from config.php file checking, which is accepted if the version is greater than a set minimum.
    -With UI and LanguagePacks, there are forward / reverse compatibility concerns.
    -The core will specify exactly which version of UI is usable, as anything newer or older may not render correctly.
    -This functionality may change in the future.
    -Right now there's only one version of UI that would work anyway, as older versions lack the version variable required for version checking.


    ADD BUTTONS
      -DEFAULT, 1 , 2
      -WIDE - 1 , 2

--------------------
COMMIT 8/8/2026
v3.6.2 - Keep working on latest UI features (Original GUI, convertGui2.php is test platform).

-v3.6.2.
-Prototype buttons for selecting the language, gui, & color are available for testing in the Original Gui.
-Add missing language packs from Original and Wide GUIs.
-Need to do...
  -Move RTL buttons to LTR (or RTL, whatever gui currently set to) so all buttons align properly.
  -Add missing colors to color buttons.
  -Add missing styles to all buttons.
  -Add a hamburger icon to hide the buttons.
  -Add the button rows to convertGui1.php.
  -Language strings variables for the rows of buttons.
-Update README.md to make the project more appealing.

--------------------
COMMIT 8/7/2026
v3.6.1 - Fix bugs in Scad on Default GUI, fix bugs in all GUIS with downloads.

-v3.6.1.
-Fix recently introduced AJAX bugs with recently update download routines.
-Add missing SCAD options from default GUI.

--------------------
COMMIT 8/7/2026
v3.6.0 - Update GUI, FFMPEG conversions, Stream conversions, add SVG conversions.

-v3.6.0.
-Work on SVG, FFMPEG operations.
 -Update FFMPEG arguments 
-Add proper input/output filtering to convertFiles().
-Update ERROR 5006 to say "The conversion from EXTENSION to EXTENSION is not supported" which is more helpful.
-Refactor input / output format filtering.
-Updated language & logic behind ERROR 5006.
-Added ERROR 25000 & 25001 for Inkscape / SVG related operations.
-Update convertGui2, all GUIs, to stop sending garbled PHP errors into the address bar on download syntax failures.
  -Makes my life hell.
  -Refactor download AJAX to accomplish this. 
  -Update HRC2-Functions.js, download_link JS function.
  -Only throw download_link when there is an actual URL to download.
-Updated Installation_Instructions.txt & file header dependency section with Inkscape reference.
-Currently the "Original" gui is unusable, as it has non-functional buttons that I'm working on.
  -This is going to go behind a settings button once it is ready.
  -These buttons will allow end-users to easily switch interface, color, & language.
  -The design is about 50% complete.
  -If you use the Original language pack, and want to keep using it on this version of HRConvert2...
    -Delete the Original folder from /UI.
    -Copy and paste the Wide folder into /UI again, rename it to Original.
    -Open UI/Original/convertGui1.php and UI/Original/convertGui2.php in a text editor.
    -Use the Find / Replace feature of your text editor to find all references to 2000px and replace with 1000px.

--------------------
COMMIT 8/7/2026
v3.5.9 - Update GUI, FFMPEG conversions, Stream conversions, add SVG conversions.

-v3.5.9.
-Add convertSVG() function with Inkscape as dependency.
-Update FFMPEG calls to improve reliabiltiy.
-Work on adding a hamburger icon to GUI with buttons for switching interface, color, & language.
-Move SVG operations from convertDrawings() to it's own routine.

--------------------
COMMIT 8/5/2026
v3.5.8 - Add language packs, update GUI.

-v3.5.8.
-Added "Verified logging environment." psuccess message to core.
-Gate the "Closing Connection." log messages behind $Verbose.
-Added 3 new color schemes to all 3 GUIs.
  -Orange.
  -Purple.
  -Dark.
-Added 8 language packs to all 3 GUIs.
  -Polish (pl)
  -Dutch (nl)
  -Swahili (sw)
  -Burmese (my)
  -Hebrew (he)
  -Aramaic (Classical/Standard, arc)
  -Assyrian Neo-Aramaic / Modern Aramaic (aii)
  -Persian / Farsi (fa)Urdu (ur)

--------------------
COMMIT 8/5/2026
v3.5.7 - Isolate OpenSCAD renders at the operating system level. Replace the source filter with a stateful scanner. Add a warning log tier.

-v3.5.7.
-Added ? to sanitizeString() function.
-Added 4 new language packs.
  -Japanese - ja
  -Indonesian - id
  -Turkish - tr
  -Vientamese - vi
-Restructuring convertGui2 so it uses functions instead of a buch of repeating code statements.
  -This work is about 50% complete. But the UI is currently in a functional state.
-Update unoconv calls to specify character encoding.
  -Update unoconv call in rc.local script to use the same character encoding.
  -If you are updating from an existing version, replace the rc.local script currently installed on the system with the one from Documentation/Build. Then reboot the server.

--------------------
COMMIT 8/5/2026
v3.5.6 - Isolate OpenSCAD renders at the operating system level. Replace the source filter with a stateful scanner. Add a warning log tier.

-v3.5.6.

-Credit goes out to the following Github users for reporting security advisories regarding HRConvert2.


  - L0stHeart  |  https://github.com/L0stHeart

    -L0stHeart reported 4 separate vulnerabilities.
      -GHSA-cwc2-f9j8-x8m2 - https://github.com/zelon88/HRConvert2/security/advisories/GHSA-cwc2-f9j8-x8m2
      -GHSA-qj74-5h4j-f368 - https://github.com/zelon88/HRConvert2/security/advisories/GHSA-qj74-5h4j-f368
      -GHSA-wg57-wvw2-9cj8 - https://github.com/zelon88/HRConvert2/security/advisories/GHSA-wg57-wvw2-9cj8
      -GHSA-f74g-4wj8-j35h - https://github.com/zelon88/HRConvert2/security/advisories/GHSA-f74g-4wj8-j35h
    -The vulnerabilities reported by L0stHeart resulted in improvements to the following areas;
      -Token & SesHash generation, salt removal & secret implementation, cleanFiles bug fixes, log storage config changes.
    -Thank you for putting time and effort into HRConvert2.
    -These advisories were very comprehensive, and represent a significant amount of work across the entirety of the codebase.
    -Sorry for coming off a little strong at first. I initially thought you were someone else.

  - Saudadeeee  |  https://github.com/Saudadeeee

    -Saudadeeee reported 4 separate vulnerabilities.
    -GHSA-qcq3-837w-2c9c - https://github.com/zelon88/HRConvert2/security/advisories/GHSA-qcq3-837w-2c9c
    -The vulnerabilities reported by Suadadeeee resulted in improvements to the following areas;
      -OpenSCAD file conversions, SCAD sanitization, SCAD operation sandboxing, & bug fixes.
    -Thank you for puttine time and effort into HRConvert2.
    -Your advisory was especially well decribed and thorough. It also came through less than 24 hours after the code was released.

-Hardened OpenSCAD sanitization routine.

-Fix unauthenticated arbitrary file read through OpenSCAD conversions.
  -Reported by Saudadeeee, with a working exploit against a live instance.
  -OpenSCAD reads any file the web server user can read & provides no sandbox of its own.
  -Unlike FFMPEG it cannot be given one through its arguments.
  -HRConvert2 relied on filtering the uploaded source & documented that filter as the boundary.
  -The filter read source one line at a time & held no lexer state.
  -OpenSCAD carries state across lines, so the two disagreed about some input.
  -A block comment closed part way through a line was inert to the filter & live code to OpenSCAD.
  -A statement whose name & bracket sat on different lines matched no filter pattern at all.
  -The filter did not register those cases as references at all, so no setting mitigated them.
  -The reporter noted that patching only the comment case would leave three others open. Correct.
  -The reporter recommended operating system isolation over a better filter. Also correct.
-Add verifyBwrap().
  -Launches a real minimal sandbox rather than checking that the bwrap binary exists.
  -Bubblewrap can be installed & still non functional where user namespaces are restricted.
  -A server that cannot isolate a render now refuses to render at all.
  -There is deliberately no fallback to source filtering, because source filtering does not hold.
-Rework convertSCAD() to render inside a bubblewrap namespace.
  -The session ScadTemp directory is the only path from the data tree that exists inside it.
  -Nothing else on the disk is visible, so nothing else can be read.
  -The network namespace is unshared, closing any OpenSCAD build whose import() accepts a URL.
  -The render writes inside the sandbox & the finished model is moved out afterwards.
  -Renders remain niced to the lowest priority & remain bounded by the conversion timeout.
-Replace the source filter with sanitizeSCAD(), a stateful character scanner.
  -The source is walked once, carrying line comment, block comment & string state.
  -One pass is required rather than two, because whichever delimiter appears first wins.
  -A line comment containing block comment characters does not open a block comment.
  -A block comment containing line comment characters does not open a line comment.
  -A keyword counts as a call only when the character before it is not part of an identifier.
  -That stops house_width, diffuser_height & include_lid from being reported as references.
  -A keyword counts as a call only when the next meaningful character is ( or <.
  -Whitespace & newlines are skipped, so a call split across any number of lines is found.
  -There is no line pairing & no fixed window, so an attacker cannot escape by adding newlines.
-Fix a fifth bypass reported against the new scanner before release.
  -Reported by Saudadeeee, again with confirmed reads against OpenSCAD 2021.01.
  -A comment placed between a keyword & its opening bracket slipped through.
  -surface/**/("file") is a live call because OpenSCAD treats a comment as a token separator.
  -The lookahead skipped spaces, tabs & newlines but stopped at the first character of a comment.
  -A comment is a TERMINATOR in the main scan loop & a SEPARATOR in the lookahead.
  -Those are different rules for the same construct & the earlier code applied only the first.
  -Comments are now skipped in the lookahead & again while reading the reference itself.
  -surface(/*x*/"file") is also handled, so a comment cannot hide the path from the resolver.
  -The reporter also confirmed include/**/<file> is a parser error in OpenSCAD & not exploitable.
-Rename the source rewriting function to rectifySCAD().
  -The function rewrites source. It does not validate it & never did.
  -sanitizeSCAD() now names the scanner, which is the function that actually inspects.
  -Neither function is described as a security boundary anywhere in the codebase.
  -The previous header claimed the filter was the only thing standing between an upload & the filesystem.
  -That claim was false & has been replaced with a description of what the code actually does.
-Rewrite whole lines rather than editing references in place.
  -Editing in place would require reproducing where OpenSCAD considers a statement to end.
  -That is the class of problem this design exists to avoid.
  -A commented out line is coarser & is impossible to get subtly wrong.
-Add warningEntry().
  -A warning starts with WARNING!!! & is written regardless of Enhanced Logging Verbosity.
  -A warning is something the administrator should know is happening.
  -A warning does not necessarily mean anything needs to change.
  -A warning carries no error number because it does not represent a failure of HRConvert2.
  -A warning can never halt execution of anything.
  -Op-Act reports normal activity. WARNING!!! reports discretion advised. ERROR!!! reports numbered errors.
-Promote four events from verbose logging to warnings.
  -An OpenSCAD source with file references removed.
  -A stream walk that denied a file, including the reason it stopped.
  -A stream file referencing a private, reserved or loopback address.
  -A stream file whose content does not match its own extension.
  -These were previously invisible on any server running with verbose logging disabled.
  -An operator could not distinguish a blocked attack from an ordinary conversion failure.
-Fix error 27004 firing on every successful conversion.
  -The recursive delete routine removes an emptied directory along with its contents.
  -The check that followed treated that absence as a failure rather than a success.
-Add error 27007 for a missing or non functional sandbox.
-Add error 27008 for a rendered model that could not be moved out of the sandbox.
-Add bubblewrap to the dependency list.
-Add an AppArmor profile & verification steps to INSTALLATION_INSTRUCTIONS.txt.
  -Installing the package is not sufficient on releases that restrict user namespaces.
  -A server may report kernel.unprivileged_userns_clone = 1 & still deny bubblewrap outright.
  -The restriction is enforced by AppArmor & is separate from the kernel sysctls.
-Tested sandbox isolation directly. Listing /etc from inside reports that /etc does not exist.
-Tested with both originally reported payloads. Both DETECTED by the scanner & both refused by the sandbox.
-Tested with the comment separator payloads. All four DETECTED after the lookahead fix.
-Tested with a two file assembly using a relative include. Resolved correctly & rendered complete.
-Tested with a thirty file real world assembly. Rendered successfully with no false positives.
-Tested with keyword shaped identifiers. include_lid, house_width & diffuser_height are not reported.
-Tested with keywords inside strings & inside both comment forms. Not reported, as intended.
-Tested four concurrent uploads & conversions in one session. Handled correctly & independently.
-Updated dependencies, added OpenSCAD & bwrap.
-Update Installation_Instructions.txt to reflect the OpenSCAD & bubblewrap setup process.
  -Update PHP install procedure.
  -Update default PHP config settings.

--------------------
COMMIT 8/4/2026
v3.5.5 - Add config.php verification, libreoffice verification, ffmpeg verification, Stream / Audio / Video ordering issues, update docs.

-v3.5.5.
-Added a config.php version check, and a supported config.php variable in verifyInstallation.
  -Add verifyConfigFile() function for this purpose.
  -Now HRConvert2 will verify that the config.php file is compatible with the core.
  -If HRConvert2 detects a config.php file mismatch, after an application update for example, the core will say so.
  -When a config.php mismatch is detected, the core will output exactly what version config is installed and which options are missing.
-Move version check from verifyGlobals to verifyInstallation.
-Add config.php sanity check that verifies that all required config entries are defined.
-Switch verifyGlobals from using the unchecked sanitizeString function to using the checked sanitize function instead.
-The GlobalsAreVerified sanity check now ensures that the sanitization checks passed.
  -Before it checked that the HRConvert2 versions matched.
-Combine cleanTempLoc() & cleanConvertLoc() functions.
-Change Document Conversion Engine from using the $HomeLoc to build the LibreOffice profile to the $ConvertLoc.
  -Now the Logs, Secret, and LibreOffice cache all lives in the non-hosted $ConvertLoc specified in config.php.
-Change the way $StreamArray is constructed from $UserStreamArray.
  -It was consuming the video and audio operations & firing before the user-intended conversion took place.
  -Now it only fires on .m3u8 & .ts files.
  -The $StreamOutputArray is constructed from the $UserMediaOutputArray & the $UserAudioOutputArray.
-Add min / max version checks to FFMPEG related operations.
-Enforces FFMPEG versions for best security & compatibility.
  -Similar in nature to the version check being done for OpenSCAD in the convertSCAD() function.
  -Variables do exist in config.php for setting the accepted (over-riding) the enforced min versions.
  -For Audio & Video conversions, the min is controlled in config.php by $MinimumFFMPEGVersion and the default is FFMPEG v4.1.
  -For Stream conversions, the min is controlled in config.php by $MinimumStreamFFMPEGVersion and the default is FFMPEG v6.0.
  -Stream conversions require higher FFMPEG levels due to security considerations & the sensitivity of handling playlist files with embedded URLs.
  -Version enforcement also prevents custom
-Update ERROR_DESCRIPTIONS.txt with information about the following error messages.
  -13, Added line to ERROR DESCRIPTION section describing new failure mode; Path failed containment.
  -14, Added line to ERROR DESCRIPTION section describing new failure mode; Path failed containment.
  -2000, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-2000, <SESSION_IDENTIFIER>: Could not verify the Document Conversion Engine installation at <FILE_PATH>!
  -2001, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-2001, <SESSION_IDENTIFIER>: The installed LibreOffice version is missing, unidentifiable, or too old!
  -11001, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-11001, <SESSION_IDENTIFIER>: The installed FFMPEG version is missing, unidentifiable, or too old!
  -12001, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-12001, <SESSION_IDENTIFIER>: The installed FFMPEG version is missing, unidentifiable, or too old!
  -21000, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-21000, <SESSION_IDENTIFIER>: The stream converter failed to launch!
  -21001, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-21001, <SESSION_IDENTIFIER>: Stream inspection denied this file. <ADDITIONAL_DATA>.
  -21002, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-21002, <SESSION_IDENTIFIER>: The installed FFMPEG version is missing, unidentifiable, or vulnerable to stream playlist protocol bypass!
  -26, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-26, <SESSION_IDENTIFIER>: Could not clean the build environment!
  -26000, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-26000, <SESSION_IDENTIFIER>: Could not remove the build environment!
  -26001, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-26001, <SESSION_IDENTIFIER>: Could not remove the development documentation!
  -27000, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-27000, <SESSION_IDENTIFIER>: Could not prepare the OpenSCAD sources for rendering!
  -27001, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-27001, <SESSION_IDENTIFIER>: Could not stage the sanitized OpenSCAD source <FILE_NAME>!
  -27002, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-27002, <SESSION_IDENTIFIER>: The OpenSCAD converter timed out after <ADDITIONAL_DATA> seconds!
  -27003, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-27003, <SESSION_IDENTIFIER>: The OpenSCAD converter failed with exit code <ADDITIONAL_DATA>!
  -27004, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-27004, <SESSION_IDENTIFIER>: Could not remove the sanitized OpenSCAD sources!
  -27005, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-27005, <SESSION_IDENTIFIER>: The installed OpenSCAD version is missing or too old!
  -27006, ERROR!!! <TIMESTAMP>, <APPLICATION_NAME>-27006, <SESSION_IDENTIFIER>: Could not read the OpenSCAD source file <FILE_NAME>!
  -28000, ERROR!!! HRConvert2-28000: The config.php file is missing <ADDITIONAL_DATA> required setting(s). Config version detected: <ADDITIONAL_DATA>. Config version required: <ADDITIONAL_DATA>. Missing Variables: <ADDITIONAL_DATA>
  -28001, ERROR!!! HRConvert2-28001: The core file reports version <ADDITIONAL_DATA> but the version information file reports version <ADDITIONAL_DATA>. This installation is incomplete or was updated incorrectly.

--------------------
COMMIT 8/3/2026
v3.5.4 - Add OpenSCAD support.

-v3.5.4.
-Add support for OpenSCAD files.
  -Added defense mechanisms against hostile includes & cyclic resource exhaustion attacks.
  -Added version check.
-Remove ancient syncLocations() function, which was dead.

--------------------
COMMIT 8/3/2026
v3.5.3 - Work on Stream functionality. Harden against log exfiltration. Harden against session hash derivation attacks.

-v3.5.3.
-Stream now requires three conversions be enabled in config.php.
  -Stream conversions now require Audio & Video conversions be enabled also.
-Update UI to dispaly output options from video files & audio files as eligible output for stream formats.
  -Applied to Default, Original, & Wide UIs & all language packs.
-config.php changes. 
  -Add AppendLogHashToLogFiles & UniqueDailyLogHash to config.php.
  -AppendLogHashToLogFiles is a boolean for controlling whether or not to append a per-server unique identifier to the end of log files.
  -UniqueDailyLogs is a boolean for controlling whether or not the unique identifier appended to log files is rotated daily or not.
  -UniqueDailyLogs requires AppendLogHashToFiles be set to TRUE in order to have any effect.
  -Remove $Salts variables.
-verifyInstallation changes.
  -Generates a secret key that is used to derive SesHash variables from a per-install secret rather than hard-coded $Salt strings in config.php.
  -Fix delete dev environment and delete changelog routine.
-verifySession() & verifySesHash() changes.
  -Added token input to log output in core section that calls verifyTokens().
  -Now Tokens contains random numbers generated using standard PHP functions at runtime instead of using stored $Salts. 
  -Now a secret is generated at install time and maintained by verifyInstallation().
  -Switch from ripemd160 to sha256, remove early truncation steps to preserve as much unique data for the final hash operations as possible.
  -All hashes used for generating unique URIs went from 12 characters to 18 characters & use SHA256 algorithm instead of ripemd160.
  -Log hash appended to log files goes from 12 characters to 18, from ripemd160 to SHA256.
  -No longer truncate URL hash operations. Leave full length hash for as long as possible. Increases entropy.
  -Token2 is now 64 characters.
  -Truncate log file operations to 18 characters to leave enough name length overhead for linux directory structure.
  -Logs were moved out of the hosted directory in v3.5.1, so probing log files has become a lot less practical now for multiple reasons.
  -Instead filename obscurity being the only line of defense against log exfiltration, now they are protected by default.
  -Filename security becomes a bonus side-dish security feature, not the main course, and can be controlled via config.php.
-verifyTokens() changes.
  -Accurately reject tokens over 18 characters in length.
-convert() changes.
  -Add support for continuing streams after the conversion has returned the output file.
-convertFiles() changes.
  -Cleaned up dead array references.
  -Reworked error handling, more robust architecture.
  -Reworked supported file detection.
  -$MainConversionErrors is now thrown on every error path.
  -$isExtensionSupported resets per file. 
  -Previously $isExtensionSupported was erroneously set once and not cleared on subsequent file loops.
  -Break addded to the format detection loop to improve performance.
  -Consolidate sources of truth for determining operation result. 
  -Error 5004 was redundant and has been removed.
  -Consilidate error checking routine. Reduce log volume & noise. 
  -Add error exception for in-process streams.
-deleteFiles() changes.
  -Fix variable assignment bug that left undeleted files.
-Add generateInstallSecret() function.
  -Generates the install secret.
-Add generateRandomNumber() function.
  -Generates fixed length random number tokens.
-Stream SSRF Protection routine changes
  -Removed all early returns. 
  -All functions now have one return at the bottom of each function.
  -This ensures memory cleanup functions as intended.
  -Also prevents code path confusion during debugging.
-Fix cleanup routine.
  -Add isDirEmptyOfUserFiles() function for telling if all files except index.html have been removed from a hosted directory.
  -Add removeEmptiedSessionDir() for determining if index.html is the only file left in a folder to be deleted.
-Fix convertAudio bitrate selection bug.

--------------------
COMMIT 8/2/2026
v3.5.2 - Harden against RCE.

-v3.5.2.
-Use sanitize() function for IP sanitization instead of hardcoded strings.
-Removve htmlentities() from sanitize() function.

--------------------
COMMIT 8/2/2026
v3.5.1 - Add SSRF protection to core. Harden against SSRF & Blind SSRF attacks.

-v3.5.1.
-Before I start this change-note I want to highlight the fact that this repository and everything in it was written by humans.
  -Half a dozen humans made this. Mostly @zelon88. Over a decade.
  -This application is a shrinking resource. Human-only codebases.
  -We as maintainers of HRConvert2 have an extraordinary responsibility to maintain the highest quality human code we possibly can.
  -This codebase will become more valuable as time progresses and human developed software becomes increasingly scarce.
  -Please continue to submit AI generated Security Advisories. 
    -I might snap at you for not running the code.
    -I may snap at you for taking 4 lines of code out of context and only fuzzing that.
    -Please have your AI agents be humble, thorough, scan EVERY file for variables it's using, and look at the ENTIRE codebase.
-The HRConvert2 Development team (me, @zelon88) received another AI generated security advisory.
  -This advisory claimed that HRConvert2 is susceptible to SSRF. It is not.
  -HRConvert2 v3.5 is not currently known to be vulnerable to server side request forgery.
  -The security advisory attempted to leverage the convertStreams() function to force the FFMPEG dependency to perform "blind SSRF" against a local LAN segment.
  -After fixing some minor bugs with the POC I tried using it to trick FFMPEG into committing SSRF.
  -I discovered that HRConvert2 prevented the attack due to a logic bug. An actual flaw in the codebase.
  -The flaw that prevents SSRF from taking place also prevents m3u8 stream conversion from working at all.
  -Before I fix the flaw and restore m3u8 functionality, I want to address the underlying SSRF issue.
-Harden the core against SSRF (Server Side Request Forgery) attacks. Specifically against ffmpeg + streams.
-Fix long-standing m3u8 conversion bug.
  -The UI offered output formats from $MediaOutputArray instead of a stream-specific array.
  -$UserStreamArray contained only m3u8, so no output format was ever eligible.
  -Every m3u8 conversion has failed at the format check on line 1656 since the feature shipped.
  -Discovered while investigating a blind SSRF advisory that turned out to be unprovable.
-Add isPubliclyRoutableIP().
  -Single source of truth for IP safety. Returns TRUE only for publicly routable addresses.
  -Replaces three separate hand-rolled checks that had drifted apart.
-Add dnsLookup().
  -Resolves a host WITHOUT following redirects.
  -Scans every returned record. A host answering with both public and private addresses is treated as a rebinding setup.
  -Discards any safe IP found if a private one appears. Stops on the first bad answer.
-Add gatherRemoteStreamHostInfo().
  -Parses one URL, enforces the scheme allowlist, resolves and validates the host.
  -Returns the host, port, scheme and validated IP used to pin CURL.
-Add resolveStreamURI().
  -Resolves relative URIs against the URL the parent manifest was fetched from.
  -Refuses relative URIs at layer 0. The uploaded file has no parent to inherit from.
-Add inspectStreamIP() and inspectStreamDomain().
  -Text-level scan for raw IPv4/IPv6 addresses and domain names.
  -Domain pattern skips user:pass@ decoys so the real host is captured.
  -Costs nothing. Catches hostile manifests before any connection is made.
-Add inspectTSFile() and classifyStreamContent().
  -Classify files by CONTENT, never by extension.
  -FFMPEG dispatches on content markers, so a .ts file opening with #EXTM3U is parsed by FFMPEG as a playlist.
  -MPEG-TS confirmed by sync byte 0x47 at every 188-byte boundary across 5 packets.
-Add downloadStreamFile().
  -Pins CURL to the pre-validated IP with --resolve. CURL never performs its own DNS lookup.
  -No -L. Redirects are never followed.
  -Protocol locked with --proto and --proto-redir. HTTP only when config allows AND the URL uses it.
  -Fetches the first 8191 bytes only. Enough to classify, not enough to be abused.
  -Saves with a numeric name and no extension so nothing downstream can be fooled by a filename.
-Add streamFileWalker().
  -Owns the queue, the seen-set, the depth counter and every budget.
  -Layer budget bounds depth. Per-layer budget bounds width. Total budget bounds the whole tree.
  -Seen-set prevents cycles from burning the budget.
  -Budget is spent BEFORE connecting, never after.
  -Halt is one-way. Nothing can clear it once set.
  -Denies immediately if a file references more URIs than the layer budget can cover.
-Add waitForStream().
  -FFMPEG now launches in the background so the user is served immediately.
  -Connection is closed, then the process is supervised and killed at $StreamWatchTimeout.
-Add config.php options.
  -$StreamInspectionLayers, $StreamInspectionFilesPerLayer, $DefaultStreamInspectionForfeitAction.
  -$MaxStreamInspectionFileSize, $AllowStreamOverHTTP.
  -HTTP is disabled by default. Admins must opt in deliberately.
  -Forfeit action defaults to DENY. An incomplete inspection is never an approval.
-Add StreamTemp to the installation verification routine.
-Tested with the m3u8 from the SSRF advisory pointing at 127.0.0.1 over http.
  -Threw error 21001, as expected.
  -Denied at layer 0 on the text scan. Files Downloaded: 0. No connection was ever made.
  -Caught independently by both the LAN check and the HTTP check.
-Note: FFMPEG v2.0 through v6.0 carry a severe vulnerability related to downloading stream files.
  -Disable stream conversions on those versions.
-Move the default logs directory to the un-hosted $CloudLoc.
-Update default $Salts.
-Need to add URL field to Stream Operation UI so users can add missing URLs.

--------------------
COMMIT 6/15/2026
v3.5 - Even further harden core.

-v3.5.
-Add additional layer of sanitization for raw $_GET color, language, and gui to verifyInputs().


--------------------
COMMIT 5/25/2026
v3.4.8 - Further harden core. Further expand usage of sanitization.

-v3.4.9.
-Add a second check for dangerousFiles in the sanitizeString() function.
-Add a second layer of trimming for dangerous characters.
-Make "Strict" basically the same level of sanitization with relaxed directory separator rules.
  -Still trim for unexpected directory separators.
-I tested sanitization with a filename of ,-..-,-,-*,o/ `>>she l5l.,php$DirSep.
  -I successfully got a file of o_she_l5lDirSep.webp.
  -Note the tab character in the middle.


--------------------
COMMIT 5/25/2026
v3.4.8 - Further harden core. Further expand usage of sanitization.

-v3.4.8.
-Remove the securePath() function.
  -It was redundant.
  -sanitizeString can do the job better.
-Improve sanitizeString().
  -Move dangerous files check to sanitizeString().
  -Hard-coded the dangerarray in-vase the global version was changed.
-Tested with already uploaded file with hostile name.
  -Threw error 14002, as expected.
-Tested with trying to set a file with a hostile name.
  -The core sanitized the name and processed normally.
-Tested uploading a file with a hostile name.
  -The core sanitized the name and processed normally.

--------------------
COMMIT 5/20/2026
v3.4.7 - Reduce 404 errors, improve user experience.

-v3.4.7.
-The core now passes the new filename back to the UI.
  -Previously the UI would try to predict the new filename.
  -This would often fail, such as when sanitization modified the filename or if the filename was blank.
  -Now the UI will always get the correct filename and won't redirect users to a 404 page anymore.
  -If an error occurs and the file cannot be downloaded, errors are displayed via an alert box instead of a 404.
-The core now properly passes blank filenames and preserves whatever the original filename was.
-Removed CSS for old IE versions from the Default UI.
  -Leave them in the Original and Wide UIs for now.

--------------------
COMMIT 5/19/2026
v3.4.6 - Improve sanitization. Harden system calls.

-v3.4.6.
-Improve sanitize() function.
  -Add chr(13), chr(0) to list of stipped characters.
  -Trim leading and trailing dashes to prevent argument injection.
-Improve verifyFile() function.
  -Add a check to ensure the UserExtension is in the $Allowed extension array.
  -Handle blank NewFilename and use the original filename.
    -This makes HRConvert2 much more batch / API friendly.
      -Try asking your AI agent to review the Documentation and then use the HRConvert2 API to convert batches of files without the UI.
-Added filename output to relevant operations so the UI can stop returning 404's when conversions fail.
  -This was started today, but will be finished in a future release, soon. Very soon.
-Give hernandito proper credit for developing the new default GUI.
  -Modify file headers of main GUI files to contain a reference to his Github profile.
  -Add a UI_CREDITS.txt file to the UI giving him credit for his time & effort.
-I received a pending security advisory which appears to be AI generated claiming that the sanitization routine was faulty allowing code injection.
  -I had a long conversation with Copilot about the Sanitization routine.
  -The pending advisory did not contain a proof of concept exploit.
  -The claims seemed unlikely due to way sanitization is used within the core.
  -I attempted to replicate the claims and was unable to do so. 
  -During testing, HRConvert2 v3.4.5 successfully thwarted every code injection attempt. 
  -I believe that v3.4.3 would also pass the same tests, as the sanitization implementation is essentially unchanged.
  -Copilot AI and I attempted to hack the code. 
  -Initially Copilot scoffed at the sanitization routines.
    -The critisizm I got from Copilot was suspiciously similar to the claims being made in the pending security advisory.
    -Initially Copilot was convinced that the routine was mortally flawed, and offered examples.
    -Each example outright failed during testing.
    -After half a dozen back-and-forth prompts Copilot was unable to gain arbitrary code execution or inject a payload into HRConvert2.
    -Copilot almost hallucinated several times before admitting that it could not actually accomplish any of the exploits it thought it could.
    -Copilot did identify that chr(13) and chr(0) were not being stripped, and reccomended stipping them.
    -The security advisory attempted to exploit the dash character as a leading character in file extensions and file names. 
    -That is very clever, although I couldn't get a PoC to work.
    -HRConvert2 passed all tests with leading dashes. 
    -The core was still hardened to eliminate any risk of leading dash abuse.
    -I stand by HRConvert2 as a secure product.
    -I believe that HRConvert2 v3.4.3+ is secure enough for public facing environments when proper network best-practices are used.
    -I invite anyone to attempt to break the sanitization routine. 

--------------------
COMMIT 5/18/2026
v3.4.5 - Preparing for Docker update.

-v3.4.5.
-Working dilligently on the Docker build process update.
-Added entrypoint.sh to contain runtime code.
-Take over maintanence of Unoconv.
  -Now HRConvert2 bundles it's own fork of Unoconv.
  -Add option called $UsePatchedDocumentEngine to config.php to specify which Unoconv to use.
-Update convertCore.php to specifically call unoconv with verbose and profile path arguments.
  -Set profile path to $HomeLoc which should be /var/www.
  -Explicitly define which Unoconv to use.
-Update docker build process.
  -Adjust dependencies to optimize compatibility.
  -Update url in ffmpeg build scripts.
  -Add file headers to ffmpeg build scripts.
  -Update Docker PHP version to php 8.5.

--------------------
COMMIT 5/17/2026
v3.4.4 - Preparing for Docker update. Fork dependency Unoconv, update code to Python 3.14+.

-v.3.4.4.
-Preparing for Docker update.
-I keep finding bugs when building the Docker version.
-Discover that Unoconv is not compatible with Python 3.14+.
  -Fork the unoconv repo to https://github.com/zelon88/unoconv.
  -Modify the unoconv file to make it compatible with latest versions of Python.
    -Remove setuptools LooseVersion calls that were causing errors.
    -Now Unoconv will not detect Office versions older than 3.3, but those are technically unsupported in v0.8 anyway.
-Include the "Patched" version of unoconv and a config entry on whether to use it or the apt version.


--------------------
COMMIT 5/12/2026
v3.4.3 - Bump JQuery to v4.0.0. Update GUI. Update documentation.

-v3.4.3.
-Added information to the file header of code files with the new dependency information.
  -xvfb-run.
  -Every time I add a dependency I re-order the list of dependencies to make them visually appealing.
-Bump JQuery to 4.0.0 released January 17th, 2026.
  -Old version was 3.6.3 from December 20th, 2022.
-Update INSTALLATION_INSTRUCTIONS.txt with.....
  -Instructions to install the latest dependencies (xvfb and libgxps-utils)
  -Adjustments to the permissions related commands to set permissions on the $ApacheUser $HomeLoc (defined in config.php).
    -Before we were focusing chown and chmod commands with -R at /var/www/html. Now we are focusing those commands at /var/www instead.
    -This change is because the www-data user needs a home directory in order to run soffice.bin and meshlabserver.
    -We want the www-data home directory to be writable to the www-data user, but not hosted (accessible over the network).
    -The home directory for the www-data user is represented by the $HomeLoc variable and is set in config.php.
    -We could probably create a $HomeLoc in /home/www-data if you want. Just make sure to give ownership and rw permissions (0755) to www-data.
-Added CODING_CONVENTIONS.txt to describe the conventions in-use in the codebase so they can be devloped transperantly alongisde the code.
-Added SUPPORTING_THE_PROJECT.txt to describe ways for inspired users to get involved with the HRConvert2 community.
-Evaluate 2 community submitted UIs.
  -The first one was very good, but missing a convertGui2.php file.
  -The second one was forked by Github user hernandito and is availalbe for download at https://github.com/hernandito/HRConvert2/tree/master.
    -Bumped the version by incorporating changes to the UI since v3.3.7.
    -I have incorporated this into the Master branch as the new "Default" UI. 
    -The old UI be accessed by appending the ?gui=Original GET variable to the URL.
    -Also added screenshots from hernandido's repository to the official readme.
-Update README.md.
  -Add the latest screenshots.
  -Adjust the list of features to include mention of the "highly customizable GUI".
-I should really work on adding buttons to the default UIs to switch between the UIs. 
  -And flag icons to switch between languages.
  -And color icons to switch between the colors (on supported UIS).
-Added an exception to ERROR 400 that prevents '.' and '..' from triggering the error and raising a false alarm.

--------------------
COMMIT 5/11/2026
v3.4.2 - Add support for OXPS documents. Lots of quality improvements.

-v3.4.2.
-Need to run 'sudo apt install xvfb' on the HRConvert2 server to install the latest dependency.
  -This is required to fix a bug in 3D model conversions using MeshLabServer.
-Improve sanitization & handling of file extensions.
  -The Gui now uses the getExtension() function from convertCore.php.
  -Added sanitization to getExtension() function.
  -Added & documented Error 300 which verifies that getExtension() was properly sanitized using the function return from the sanitize() function.
  -Made getExtension() and getFiles() functions more robust by initializing variables and adding sanity checks.
  -Added & documented Error 400 which verifies that the getFiles() function does not return an unsafe file.
-Fix 3D Model conversions.
  -These were getting mixed up with .stl subtitle conversions.
  -Added an output format check to the loop that executes the conversion to prevent this collision of formats.
  -Now only the desired conversion will take place.
  -Discover that new versions of Meshlabserver require the www-data user to have a UI environment.
    -To get around this, use xvfb-run to create a virtual environment.
    -Also call the meshlabserver binary by absolute path instead of assuming it is in PATH.
-Fix subtitle conversions.
  -Adjust the input / output formats. Added some, removed others.
  -Fix collision with .stl subtitle formats triggering the 3D model converter.
  -Now only desired conversions take place.
-Add 'xvfb-run' as a dependency.
  -The current list of dependencies that need to be added to the Docker repos are.....
    -xvfb-run
    -libgxps-utils
  -Need to update the Docker repos, very soon.
-Replace individual queries for file extensions with getExtension() function calls.
  -This improves consistency of the code and ensures proper sanitization wherever the extension is used.
  
--------------------
COMMIT 5/8/2026
v3.4.1 - Add support for OXPS documents. Lots of quality improvements.

-v3.4.1.
-Users who are upgrading can install new dependencies and adjust folder permissions by.....
  -Install libgxps-utils with the 'sudo apt install libgxps-utils' command.
  -Grant ownership of the /var/www folder to the www-data user using the command 'sudo chown www-data:www-data /var/www'
    -This is because this folder acts as the www-data home folder when HRConvert2 is running. 
    -The Document Conversion Engine (soffice.bin) needs a home folder that is writable in order to function.  
-Add support for OXPS documents.
  -As requested by Github User physics515 in issue #86, https://github.com/zelon88/HRConvert2/issues/86.
  -Experimented with Ghostscript XPS support and also libgxps-utils and determined that libgxps-utils is the most suitable candidate for adding XPS and OXPS support to HRConvert2.
  -New dependency can be added by running 'sudo apt install libgxps-utils' 
  -Add libgxps-utils to the the depency text in the file header for code files.
  -Put OXPS related code under Presentation array.
-Switch the singular PresentationArray to PresentationInputArray and PresentationOutputArray to control which options that are displayed by the UI & when.
  -This also required modifying lines 282, 798, and 806 in the UI/Default/convertGui2.php and UI/Wide/convertGui2.php files.
-Fix some typos in comments.
  -Core, LanguageStrings.
-Fix bug in the verifyInstallation() function for optionally cleans up build and/or development environments.
  -The checks being performed to decide whether or not to execute cleanup were inadvertantly mutually exclusive.
  -Before the code for cleanup of either build environment or dev docs would only happen if both were enabled.
  -Now the checks are truly independant.
-Move the Document Conversion Engine cache location ($HomeLoc) from /var/www/html/HRProprietary to /var/www
  -Also added reusable code to cleanup old .cache and .config locations that may exist.
    -$RequiredCleanupFolders specified in verifyGlobals().
    -More information below.
-Improve syncLocations() function.
  -Flip to use chown then chmod (was chmod then chown).
  -Remove ancient $MAKELogFile variable reference.
    -This was probably the oldest piece of code in the application until now.
-Improve verifyRequiredDirs() function.
  -Add code to cleanup unneeded folders. From previous versions for example.
  -The array for this is contained in the verifyGlobals() function.
  -Added $ProprietaryLoc entry to config.php.
-Update Language Strings files.
  -Add new code for XPS files.
  -Fix Static (always English) code for Presentation files.

--------------------
COMMIT 5/5/2026
v3.4 - Strengthen sanitization process. Improve OCR, Document Conversions.

-v3.4.
-Further strengthen the sanitization process to prevent potential command injection.
  -Github user KhaelK138 discovered another sanitization vulnerability, this one as severe as the last one.
  -Added chr(10) to the lists of sanitized characters in both strict and non-strict modes.
  -GHSA-f74g-4wj8-j35h
-Improve OCR operations.
  -More verbose logging, when Verbose is enabled in config.php.
  -Minor corrections to code spacing and grouping.
  -Added Error 15010 "Could not verify the document conversion engine...".
  -Added Error 15011 "Could not create a file at ..." for when OCR operations fail to generate required files.
  -Fixed a bug where the error checker was nested too high and would only really check the last operation performed by the loop.
    -Typically only one file operation is performed at a time with the standard GUI, but the core can do batches of files. Well, now it can ;).
-Discovered that document conversions won't work if soffice.bin is running as another user.
  -Added checks and a mitigation for this in the verifyDocumentEngine() function.
  -Previously the verifyDocumentEngine() function would not check who the owner was.
  -Now verifyDocumentEngine() function will check who the owner is, and if it is not root or $ApacheUser will attempt to start a new instance.
-Improve whitespace consistency.
  -Remove whitespace chars after opening brackets.
-Improve comment consistency.
  -Make sure that when code snippits appear multiple times that the comments sections match.
-Add several new comment sections, elaborate on several others.
-Remove unused $returnData variable references in the userVirusScan() function.
-Updated config.php.
  -Added $HomeLoc variable for the $ApacheUser home folder.
  -Added $DocumentEngineSleepTimer to determine how long to wait after trying to start the Document Conversion Engine.
-Added a line of code to the verifyGlobals() function to specify the home folder for the $ApacheUser to be the $HomeLoc.

--------------------
COMMIT 5/3/2026
v3.3.8 - Address critical vulnerability in sanitization process.

-v3.3.8.
-This commit is a response to Github Security Advisory GHSA-f74g-4wj8-j35h submitted by Github user KhaelK138. 
-You can read the original security advisory at https://github.com/zelon88/HRConvert2/security/advisories/GHSA-f74g-4wj8-j35h.
-KhaelK138 reported this issue via the official Github repository on April 22nd 2026.
-KhaelK138 sent me (zelon88) an email on May 3rd 2026 to follow up.
-I verified that the reported vulnerabilities exist.
-I have implemented KhaelK138's suggested fixes.
-I have requested a CVE for this vulnerability from Github.
-The vulnerability potentially allows remote attackers to achieve remote code execution on the HRConvert2 server via two different methods;
  -Method 1: Attackers could potentially leverage the backtick character to inject arbitrary commands that execute in the context of the www-data user.
  -Method 2: Attackers could potentially leverage the tab character to drop files remotely into hosted locations on the server.
-The severity of these vulnerabilities could result in a complete hostile takeover of the server by a remote attacker.
-The vulnerability impacts all versions of HRConvert2 prior to v3.3.8.
-It is HIGHLY RECCOMENDED that all versions of HRConvert2 prior to v3.3.8 be updated to v3.3.8 immediately.
-You can verify the version of your HRConvert2 installation by navigating to your HRConvert2 installation directory (usually /var/www/html/HRProprietary/HRConvert2) and opening the Documentation/CHANGELOG.txt file with a text editor. The version information is contained in the top header section of this file.

--------------------
COMMIT 5/22/2024
v3.3.7 - Add support for 357 formats. Update build process.

-v3.3.7.
-Clean up config.php comments.
-Delete "SUPPORTED_FORMAT_INFORMATION.txt" development work file.
-Finish updating and adding support to the list of supported formats using the latest ffmpeg build process.
-Added support for ~357 file formats. The total of supported formats is now up to 445. (!!!)
-Update INSTALLATION_INSTRUCTIONS.txt to reflect the ffmpeg build-from-source procedure.
-Create DOCKER_BUILD_INSTRUCTIONS.txt for me to remember how to build docker images.
  -I typically develop this stuff on VMware with installed locally dependencies.
-Update Dockerfile with the latest ImageMagick and FFMPEG build process.
-Fix Docker build process, update Docker Hub.

--------------------
COMMIT 5/22/2024
v3.3.6 - Add pdf to image support, cbr, cbz support to archive support.

-v3.3.6.
-Special thanks to:
  Ask Ubuntu User:  Lucas Walter
  https://askubuntu.com/questions/1127260/imagemagick-convert-not-allowed
  https://askubuntu.com/users/27302/lucas-walter

  # Open the file 
  sudo nano /etc/ImageMagick-6/policy.xml

  # find and edit the line
  <policy domain="coder" rights="none" pattern="PDF" />
  # to :
  <policy domain="coder" rights="read|write" pattern="PDF" />

-The above code will allow pre-installed versions to convert pdf files as images in addition to documents and OCR.
-Added CBR and CBZ support. 
  -They are just zip files full of jpeg images.
  -We treat them as .zip files, but we use 7z to extract them because zip doesn't work for some reason.
  -After they are converted to a proper archive we can convert them further.
  -It could be possible to improve quality of the jpegs by creating a dedicated convertComics() function with direct output formats.
  -Can we get the comic community to show the project some love to get this done? ;)
  -Update the installation instructioins to allow PDF conversions as images.


--------------------
COMMIT 3/24/2024
v3.3.5 - Rar on RPI fix, FFMPEG build process changes.

-v3.3.5.
-Continue ScanCore refactor.
  -v1.1.
    -Added support for -ud argument, which will Update Definitions!
    -This gets defs by default from the ScanCore_Definitions repository.
    -Defs are now broken into subscriptions.
    -Subscriptions include Virus, Malare, & PUP.
    -Each client will download only the subscriptions that are specified in config.php.
    -The client will then compile its subscribed definitions into a "combined" definitions file locally.
    -This will give users the ability to control which definitions they install, controlling what ScanCore will detect.
    -Because ScanCore is portable, that means you can set different scanners to do different things.
    -Need to work on a way to automate definition updates. Every scan, daily, weekly, bi-weekly, or monthly.
  -v1.2.
    -Add 'raw' and 'git' update methods. So if you can't get git (hehe) then you can still perform updates.
    -Use globals $EOL, $SEP for PHP_EOL and DIRECTORY_SEPARATOR, as intended.
    -Add -version argument for displaying useful info.
      -Info like the source for def & app updates, the original repo, subscribed defs, version, last update, ect...
    -Added a plug to the LICENSE INFORMATION section of the header text.
      -// / BSD or MIT licensing is available. Reach out to @zelon88 for more information.
      -Support your favorite developer by buying a trophy license for your favorite open-source software.
    -Imrove comments / formatting in ScanCore_Config.php. Add default values, labels, better descriptions for everything.
    -Add -help, -h and -version, -ver arguments for showing version & help, respectively.
    -Add -configfile, -cf argument for selecting which configuration file to use.
      -Now one scanner can rule them all!
      -Instead of installing multiple scanners to scan different subscriptions, just have different config files.
    -Revise output. Make more consistent, easier to read.
    -Remove unused $LogFile and $LogDir references from core.
    -Remove unused $LogFile and $LogDir references from config.
    -ReportsDir is now the only location for logs. Logs & reports are officially the same thing.
    -The -logfile & -lf arguments are now undocumented aliases of the -reportfile and -rf arguments.
    -I plan on removing -logfile & -lf arguments in the future.
    -What will probably happen is I'll forget I said that and wind up re-documenting these undocumented aliases in the future.
  -v1.3 - Add -defsfile, -df argument. Reorder logic & functions. Clean up output. Improve help, version output.
    -Add -defsfile & -df argument for specifying a definition file by command line. 
    -Now ScanCore is completely modular. It can be run against any defs file and any config file, so automating tasks is easy & granular.
    -Add config file last modified, which core file, and which defs file to version output.
    -Add new commands to help output.
    -Will update the docs with proper error descriptions and instructions once the error numbers stop chaning.
-Thanks to @bit-man for submitting a fix for rar file support on RPI & non x86-64 platforms.
-Evaluate how we're using ffmpeg, per issue #59, but also effects issue #29.
  -Remove M4P support.
  -Omg there's even a typo... file format oog should be ogg.
  -Change ffmpeg build process to build from scratch.
  -Inprove capability & file support. Considerably.
  -Increase duration and difficulty of install process. Considerably.
  -Start by reverse engineering https://raw.githubusercontent.com/markus-perl/ffmpeg-build-script/master/web-install-gpl-and-non-free.sh.
  -Build process complete as of 3/25/2024. 
    -Now we need to incorporate the new ffmpeg functionality into core.
    -Then we can update the build / install process & documentation.
-Add the selected GUI, Language, and Color to error or verbose success messages to enhance debugging potential.
-Update ERROR_DESCRIPTIONS.txt with the new information.
-This version is just the start of an overhaul to media file processing.
-Add some temporary files to DOCS for helping me track my ffmpeg overhaul progress.

--------------------
COMMIT 3/22/2024
v3.3.4 - ScanCore refactor. Update code structure, improve config.php, rename variables.

-v3.3.4.
-Added missing loop in sanitizeStrings() function.
-Refactoring ScanCore to bring it up to the same level of code quality as the rest of the project.
  -ScanCore to v1.0, Do away with defs versioning.
  -Definitions can be versioned by date.
-Remove false positive for jquery 3.6 minified from defs.
  -Pretty sure this is the 20th anniversary for the original PHP-AV codebase, which there is very litte left.
-Improve config.php by adding more variables to it.
  -MemoryLimit, ChunkSize, Debug, Verbose.
-Make the file headers more consistent.
-Once the quality is up to par we will focus on adding features and capability.
  -Specifically an auto-updater would be nice.
  -Then maybe some automation tools for scraping IOCs and formatting them into the definitions file.
-Recursion is now disabled by default.
  -This affects behaviour of scripts that use ScanCore because now you HAVE to specify if you want recursion or scans will fail. 

--------------------
COMMIT 3/12/2024
v3.3.3 - Misc cleanup. Update developer docs w/API information.

-v3.3.3.
-Create official Docker image.
  -https://hub.docker.com/repository/docker/zelon88/hrconvert2
-Delete UI/Default/Resources/CSS/basic.css.
-Delete UI/Wide/Resources/CSS/basic.css.
-Update Documentation/CREATING_GUIS.txt to better communicate the API, including POST and GET requests.
-Replace "APPLICATION INFORMATION" with "COPYRIGHT INFORMATION" in the header of each code file.
-Move Dockerfile to Documentation/Build.
-Adjust core logic that cleans up the Dockerfile with the new location.
-Add header text to the Dockerfile consistent with the project.
-Update Dockerfile to copy the index.html file to HRProprietary and HRConvert2 installation folders.
-Add logic to the core to provide the option of using alternative software for handling rar files in the future.
  -The core will check for config.php to see what $RARArchiveMethod is set to, and then check for the existence of /usr/bin/rar
  -This is mostly for development so I can easily test alternatives to the rar software that we're stuck with.
-Update dependencies in installation instructions.
  -Add lots of missing dependencies.
  -Call out mkisofs and tar explicitly because who knows what kind of docker images people are starting with out there.
  -Add requirement for p7zip-rar.
-Thanks to @bit-man for help on Issue #60.
  -bit-man single handedly solved the rar problem by figuring out how to get non-free repositories into the docker image.
-Added a config.php entry for controlling how many times the core will retry significant file operations.

--------------------
COMMIT 3/8/2024
v3.3.2 - Add green check, red X where loading spinner goes after a file operation is complete.

-v3.3.2.
-Fix bug where the loading spinner would never disappear for archive operations.
-Update docker URL in installation instructions.
-Add victory and failure icons where the loading spinner goes for 5 seconds after a file operation.
-Add Wide GUI.
-Fix bug with GUI selection where the GET input was case sensitive, but the core was forcing it to be interpreted as lower case.
-Fix bug with GUI selection variables not being scoped properly.
-Make colors user selectable.
-Add missing div tag to convergGui2.html.
  -I can't find any HTML erros in console anywhere. Yipee!!!
-Utilize POST and GET for language, GUI, and color inputs.
  -Utilize the standardized sanitization procedure for these inputs.
  -It was somewhat ad-hoc before, even though the global sanitized variables were already pre-prepared.

--------------------
COMMIT 3/7/2024
v3.3.1 - Reorganize folders, create docker file, consolidate UI.

-v3.3.1.
-Move a bunch of files.
  -Move versionInfo.php and config.php into the Resources folder.
  -Create build folder.
  -Move rc.local, rc.local.service, and a php.ini sample file in there that will be used for Docker installs.
  -Move all resources into the UI\Default folder.
  -Relieve the core of the logic of defining CSS, JS, and images for the UI pack. That's not very modular.
  -Now the UI pack defines these in header.php.
  -Resources folder is not a required part of a UI pack. So the core won't check for it, 
  -Although the core does define a relative path where the Resources folder, and JS, CSS, Images subfolders should go as a convinience.
  -It is not required by the core to have these folders, or to organize your theme into them.
  -Remove LICENSE file from the root of the repository, as this is also in the Documentation folder. 
-Add a return value to cleanFiles() because I need one for the changes to verifyInstallation().
  -Use the return value to make the variables $CleanedConvertLoc and $CleanedTempLoc meaningful by passing the return value to them.
-verifyInstallation() now checks config.php for $DeleteBuildEnvironment, and if true will delete the /Build folder.
  -This is enabled by default to prevent attackers from using this to enumerate configuration information about the server or application.
-Improve documentation, formatting of styleCore.php for consistency/readability.
-Continue the long, ardous task of converting all the double quotes in convertGui2.php to single quotes.
  -This will take a while, but I have done enough to be able to find/replace what I need.
-Remove Error 2-1 from ERROR_DESCRIPTIONS.txt because it is no longer generated by the application.
-Move supported formats to config.php so server administrators can control their own fate, if they dare.
  -Default values are also listed in config.php if needed.
-Add support to remove the build environment & "development docs" that give away version info like readme.md & changelog.txt.
-Changed default color scheme from grey to blue because grey is especially gross looking to me right now.
-Fix bug where font defined in config.php was not propagating to styleCore.php.
-Fix bug with user virus scans when using ClamAV to scan an individual file for viruses. 
  -The scan would complete, but it would utilize both ScanCore and ClamAV when all the user asked for was ClamAV.
-Move default installation location to /DATA/HRConvert2 to make it consistent for Docker implementation.
-Start working on official Docker version.
-Minor update to installation instructions, consistency, consolidate steps.
-Added icon files to the Resources directory to be used in a future update.

--------------------
COMMIT 3/5/2024
v3.3 - Update Readme, fix cleanup bug, flip button CSS.

-v3.3.
-Update README.md.
  -Remove mention of Docker repo, as it is not maintained.
  -I appreciate all of the community feedback at https://github.com/zelon88/HRConvert2/issues/46.
  -I will come up with an official Docker solution.
-Fix bug in cleanFiles() function that was causing problems with archive conversions & preventing cleanup.
-Flip button CSS, so buttons don't appear quite so... upside-down... all the time.
-Remove un-needed (redundant) button related CSS from HRConvert2.css.
-Increase default DeleteThreshold in config.php from 30 to 60 minutes.
-Remove un-needed extension being passed to the convertDrawings() function.
-Fix drawings not returning a valid filename.
  -Conversions would complete, but not download.
-Tested ImageMagick support for SVG, could not get it to work.
-Tested Dia support for converting things into PNG, BMP, and JPG. Got PNG to work, so added support.

--------------------
COMMIT 10/30/2023
v3.2.9 - Improve docs, improve quality, update KO language pack.

-v3.2.9.
-Happy Halloween!
  -I started this commit a couple months ago and just got around to finishing it.
  -The next commit will hopefully be a small quality commit / release v3.3.
-Fix missed translation in Arabic language pack (languageStrings.php) for Default GUI.
  -'Archive Formats'
-Fix spelling, consistency errors in comments of config.php.
  -No logical changes, just comments.
  -'fules' instead of 'files'.
  -Standardize some comments around the verbiage 'Provide users with the option to'. 
-Fix bug in verifyDocumentConversionEngine() function.
  -This bug would have prevented the verifyDocumentConversionEngine() function from starting anything.
  -This would cause document conversion errors in cases where the rc.local steps of the installation instructions were not implemented.
-Update it, ko, pt, ru, uk, zh language packs.
-Fix bug with displaying number of files that were uploaded.
  -Move logic that calculates this to before the languageStrings.php call.
-Fix bug in convertGui2.php where the download button would produce a 404 unless the share link had been generated already.
  -Missing ajax request type: post.

--------------------
COMMIT 5/3/2023
v3.2.8 - Continue implementing separation of UI from language packs.

-v3.2.8.
-A UI now contains the language packs for the UI.
-A UI now contains it's own styleCore.php file containing stylesheets for each color.
-The UI is now completely modular!
-Multiple UIs can now be installed simultaniously and selected by the user via GET request.
  -Adding supported GUIs is the same process as adding supported language packs.
-Add "CREATING_GUIS.txt" file to 'Documentation' folder.
-update "CREATING_LANGUAGE_PACKS.txt" to reflect the new locations & GUI / language pack format.
-Add languageStrings.php to Italian language pack.
  -Language packs complete:
  -Note that h,1,2,f are not needed anymore because they are all the same, stored in \UI folder.
    -Ar l
    -Bn l
    -De l
    -En l
    -Es l
    -Fr l
    -Hi l
    -It l
    -Ko
    -Pt
    -Ru
    -Uk
    -Zh

--------------------
COMMIT 4/26/2023
v3.2.7 - Implement separation of UI from language packs.

-v3.2.7.
-Clean up error procedures and error descriptions in delete function.
  -Cleaned up spacing, documentation inconsistencies, incomplete error messages.
-Updated CREATING_LANGUAGE_PACKS.txt to be more informational.
-Add verifyGui() function.
  -Mostly copy verifyLanguage() function.
  -Add ERROR 25, "Could not verify GUI!".
  -Add ERROR 25 to ERROR_DESCRIPTIONS.txt.
-Move UI files to UI folder in root of installation location.
  -Now the loading spinner, colors, language, and UI theme will be adjustable.
  -Language and UI theme will be user selectable.
  -Need to add config entries, other supporting logic.
  -Need to update language pack docs.
  -Need to update api information.
  -Need to update config.php
-Add languageStrings.php to Hindi language pack.
  -Language packs complete:
    -Ar l,h,1,2,f
    -Bn l,h,1,2,f
    -De l,h,1,2,f
    -En l,h,1,2,f
    -Es l,h,1,2,f
    -Fr l,h,1,2,f
    -Hi l,h,1,2,f
    -It
    -Ko
    -Pt
    -Ru
    -Uk
    -Zh

--------------------
COMMIT 4/18/2023
v3.2.6 - Add Spanish, French languageString.php file.

-v3.2.6.
-Remove duplicate global variables from buildGui() function in convertCore.php.
-Create UI folder in root HRConvert2 directory.
-Move UI pages to UI folder.
-Update languageStrings.php files to have $CoreError variable.
  -ar
  -bn
  -de
  -en
  -es
  -fr

--------------------
COMMIT 4/17/2023
v3.2.5 - Add Spanish, French languageString.php file.

-v3.2.5.
-CONCEPT EXPLORATION: I Want to make UI's all exactly the same.
  -The quest for modularity / scalability / maintainability continues!
    -Making the UI and language packs separate, we enable the development of UIs to be language-agnostic.
      -Add an errorStrings.php to each language pack.
        -Define log language + error language in config, use errorStrings to translate core errors also.
      -Add error translations to the ERROR_DESCRIPTIONS.txt file in a
      -If we develop a a UI, it will apply to every language pack.
      -If we develop a language pack, it will apply to every UI.
      -UI developers can add/require their own supplemental languageStrings files, or bake this text into the UI.
      -UI developers can chose to completely ignore the stock languageStrings file if they want.
  -Create new root folder called UI.
  -Move h,1,2,f files into UI folder.
    -Create a subfolder for existing UI called Default.
    -Move styleCore and stylesheets.css into UI/Default folder.
    -Future ideas for UIs include "dark" "light" and various holiday themes.
      -Maybe an automatic holiday selection mechanism based on timezone?
  -Move errors from h,1,2,f to languageStrings.php.
  -Make core check for existence of h,1,2,f files in the language pack folder.
    -If style is specified & user style selection is allowed, use specified style.
    -If h,1,2,f files exist and no style specified, use UI/Default.
    -If h,1,2,f files don't exist and no style specified, use UI/Default. 
-Tweak German language pack.
  -Remove extra whitespace in UI1 buttons to save room.
-Add languageStrings.php to Spanish, French language packs.
-Language packs complete:
  -Ar l,h,1,2,f
  -Bn l,h,1,2,f
  -De l,h,1,2,f
  -En l,h,1,2,f
  -Es l,h,1,2,f
  -Fr l,h,1,2,f
  -Hi
  -It
  -Ko
  -Pt
  -Ru
  -Uk
  -Zh

--------------------
COMMIT 4/14/2023
v3.2.4 - Misc cleanup.

-v3.2.4.
-Merge security related checks in file operations inside the error check code.
  -Try to make them all consistent. Move some log code to after the sanitization process in upload/download/delete.
-The log message 'Could not verify tokens!' will now only appear in logs if $Verbose is set in config.php.
-Fix missing translations in subtitles, subtitles labeled as models.
  -Was leftover copy-pasta.
-Add jpe to supported formats.
-I really want to rework PDFs soon. 
  -Want to add the ability to convert between any supported format using existing functions.

--------------------
COMMIT 4/13/2023
v3.2.3 - Continue working on languageStrings concept, enable subtitle conversions.

-v3.2.3.
-Implemented subtitle conversions for Arabic, Bengali, German, & English language packs.
-Work on German language pack.
  -Copy English as a template.
-Language packs complete:
  -Ar l,h,1,2,f
  -Bn l,h,1,2,f
  -De l,h,1,2,f
  -En l,h,1,2,f
  -Es
  -Fr
  -Hi
  -It
  -Ko
  -Pt
  -Ru
  -Uk
  -Zh

--------------------
COMMIT 4/5/2023
v3.2.2 - Continue working on languageStrings concept.

-v3.2.2.
-Language packs complete:
  -Ar l,h,1,2,f
  -Bn l,h,1,2,f
  -De
  -En l,h,1,2,f
  -Es
  -Fr
  -Hi
  -It
  -Ko
  -Pt
  -Ru
  -Uk
  -Zh

--------------------
COMMIT 4/4/2023
v3.2.1 - Continue working on languageStrings concept.

-v3.2.1.
-Work on Arabic language pack.
-Add $GUIAlignment variable to control the float of UI elements.
  -This is needed to make lists presentable in right-to-left languages.
-Add dynamic supported format counting instead of hard-coding the number of formats for each UI.
  -Previously we would count and adjust the number of unique conversions that are supported, then hard code this value into each UI.
  -Now the core counts the number of supported formats and uses this value when building the UI.
  -This is more maintainable, and more accurate.
  -Now each converter will reflect the number of formats that are actually supproted.
  -So if an administrator disables all conversions except for model conversions, there will be a very small number of supported conversions reflected in the UI.

--------------------
COMMIT 3/28/2023
v3.2 - Continue working on languageStrings concept.

-v3.2.
-Implement dynamic supported format list in UI1 (english language pack only).
  -The UI will now only display supported formats that are backed by logic.
  -The lists of supported formats contained within the UI no longer need to be updated.
  -The lists of supported formats are now entirely derived by logic.
  -Still need to add OCR related formats when that refactor takes place.
  -Need to keep going on UI2.
-Added version verification to convertCore.php.
  -The logic is split between verifyInstallation() and verifyGlobals(). 
  -verifyInstallation() verifies versionInfo.php and loads it.
  -verifyGlobals() performs the actual version check.
  -Modified ERROR_DESCRIPTIONS.txt to match these changes.
  -Added ERROR 24000 when unable to load the versionInfo.php file.
-Updated ERROR 11.
  -Added information about the new version check.
-Continue changing double quotes in HTML to single quotes.
  -Header is done.
  -UI1 is done.
  -Footer is done.
-Move paths for scripts, stylesheets, javascript & favicon to verifyGlobals().
-The name of the archive when a user selects to compress all files now reflects $ApplicationName variable.
  -This was previously hard-coded to "HRConvert2_Files-<DATE>".
-Added mysteriously missing bitrate UI features.
-Adjust the list of supported formats to be more reliable, predictible, maintainable.
-Only set FormatArray variables for the SupportedConversionTypes enabled in config.php.
  -Now the arrays aren't even populated unless the conversion type is enabled.
-Move "The user selected..." text to the beginning of the upload and download functions.
-Prevent the upload of unsupported file formats.
-Prevent the download of unsupported file formats.
  -Add ERROR 3004 "Unsupported File Format" to ERROR_DESCRIPTIONS.txt.
  -Copy ERROR 6001.
-Add extra line of detail to error 6001: "The file that causes this error will be skipped."
-OCR operations now require that both OCR and Document conversion types are allowed in config.php.
-Stream conversion operations now require that both Stream and Audio conversion types are allowed in config.php.
-Add a $GUIDirection variable to languageStrings.php that determines the reading order of the UI.
  -Can either be "ltr" for "left to right" or "rtl" for "right to left".
  -A default is defined in verifyGlobals() but this is overridden later by languageStrings.php.
  -This should make migrating old language packs easier.
-Add delete functionality to core logic section & UI.
  -Add ERROR 24, Delete Failed.
-Disable loading spinner on error return in UI2.
  -Before the spinner would remain on error.

--------------------
COMMIT 3/20/2023
v3.1.9.9 - Continue working on languageStrings concept.

-v3.1.9.9.
-Standardize logic format across all UI elements.
  -Header. UI1, UI2, Footer.
  -They all declare that they are loaded and error when the core is not loaded.
  -The rest of the error logic is contained within languageStrings.php.
  -Per language pack, the logic is only contained within the code once. Instead of once for each UI file.
-Start moving UI1 text to languageStrings.php.
-Adjust the format of languageStrings.php.
-Add icon for future delete functionality.
  -https://www.iconarchive.com/show/koloria-icons-by-graphicrating.1.html
-Update subtitle icon.
  -https://www.iconarchive.com/show/koloria-icons-by-graphicrating.1.html

--------------------
COMMIT 3/9/2023
v3.1.9.8 - Clean up core variable assignments. Start working on delete functionality.

-v3.1.9.8.
-Taking a break from languageStrings to work on the core a bit.
  -Need a change of pace.
-Added deleteFiles() function for removing files manually (before they are deleted automatically).
-Preparing for the next round of UI feature updates.
  -Subtitle conversions.
  -Delete files manually.
-Added errors 23000, 23001, 23002.
-Reordered errors so they are sequential in the ERROR_DESCRIPTIONS.txt file.
  -Before they went in logical order as they are written in convertCore.php, which is confusing unless you know the code.

--------------------
COMMIT 3/9/2023
v3.1.9.7 - Continue working on languageStrings concept.

-v3.1.9.7.
-Keep working on english GUI2.
-Make buildGui() function with comprehensive checks to ensure that things are loaded properly.
-This commit will still cause Error 16 because I've only completed one language pack.
  -Hopefully once this is done to all language packs things will be more maintanable.

--------------------
COMMIT 3/9/2023
v3.1.9.6 - Continue working on languageStrings concept.

-v3.1.9.6.
-Development Version.
  -Language packs other than English will not work.
  -English language pack is unreliable.
  -OCR operations need attention.
-I screwed up and posted the last batch of commits (v3.1.9.5) to to the wrong branch! 
  -Then got frustrated and deleted the development branch.
  -Now that I'm not as frustrated I will try again.
-Reduce the amount of HTML tags that goes into languageStrings.php.
  -This is needed because of variable scope. 
  -The file loop in convertGui2.php doesn't update variables contained in languageStrings.php due to where it is called in.
-Start converting the UI HTML to single quotes wherever possible.

--------------------
COMMIT 3/7/2023
v3.1.9.5 - Continue working on languageStrings concept.

-v3.1.9.5.
-Added an official Development branch to the official repo.
  -The reason for this is because I'd like to stage updates on the Development branch until they are ready.
  -Then the master branch will get updated only to stable versions.
  -This is required because it has been taking me several commits to fully implement several features/fixes/improvements.
  -I always try to have an error free program at commit time, but still; new users should not be exposed to an incomplete versions.
-Shortened verifyGlobals() in convertCore.php by assigning multiple variables of the same value at the same time.
-Add alt and title text to english language pack, download button.
  -Will continue adding the rest.
-Updated CREATING_LANGUAGE_PACKS.txt file.
  -Expanded on --About Language Packs-- section.
  -Added --Maintaining Language Packs-- section, in case anyone else out there gets super ambitious ;)
  -Added --Development Tips-- section.
  -Added info about languageStrings.php file.
  -Revised the wording for the rest of the files.
-Add array check in verifyLanguage() function to $SupportedLanguages array.
-Fix bug in verifyLanguage.php caused by misplaced bracket (with matching bad indentation). 
  -Would have caused problems during reverting to default language if $AllowUserSelectableLanguage was set to FALSE.

--------------------
COMMIT 3/6/2023
v3.1.9.4 - Work on languageStrings concept - separate language specific UI & logic from base UI elements.

-v3.1.9.4.
-The goal of moving the language specific text and logic from the base UI elements is that it becomes easier to maintain language packs.
  -Currently every time I release an update that modifies a language pack, I must duplicate that update 14 times and change relevant language related text.
  -By separating this and itemizing it in variables contained in a new languageStrings.php file, maintaining things and adding features becomes easier.
  -Also, the base files header, convertGui1, convertGui2, and footer become universal. Meaning that changes to them can be easily duplicated.
  -We could simply get rid of these from the language packs and simplify a language pack down to just the languageStrings file.
  -We can explore that later.
-Update english language pack, working on formatting new style for all language packs.
  -Move all language related logic to new languageStrings.php file.
-Improve ERROR 16 description in ERROR_DESCRIPTIONS.txt.
-Make verifyLanguage function more robust.
  -Add detection for if the root Language folder exists.
  -Utilize global variables for required directory detection.
-Add global variables to contain language pack file paths.

--------------------
COMMIT 3/2/2023
v3.1.9.3 - Continue updating language packs.

-v3.1.9.3.
-Work on it, ko, pt, ru, uk, zh language packs.
-Keep numbering language packs to v3.1.9.1.
-Finally finished v3.1.9.1 language packs!!!
-Remove PDF from imagearray.
-Update "CREATING_LANGUAGE_PACKS.txt" to remove duplicate API inputs.

--------------------
COMMIT 2/27/2023
v3.1.9.2 - Continue updating language packs.

-v3.1.9.2.
-Work on es, fr, hi language packs.
-Keep numbering language packs to v3.1.9.1.
-Update language pack update procedure.
-Add "languageStrings.php" file to english language pack as a placeholder.
  -Will eventually migrate all locale specific text strings and direct formatting to this file.
  -With all strings separated from the UI it will be easier to maintain the UI.
  -We could keep separate h,1,2,f files or keep a master set and only load the languageStrings file.
  -If we keep separate people can modify their language easier. Also makes lh/rh reading order easier.
  -If we use a master set we reduce size and complexity.

--------------------
COMMIT 2/21/2023
v3.1.9.1 - Continue updating language packs.

-v3.1.9.1.
-Work on bn, de, en, ar, es, fr language packs.
-Update language pack update procedure.
-Update File Information section header text missing dots.
-Evaluate subtitle conversion options.
  -https://ffmpeg.org/ffmpeg-all.html#Subtitle-Formats
  -https://superuser.com/questions/1090989/changing-subtitle-format-with-ffmpeg
  -WebVTT X X X X
  -SSA/ASS X X X X
  -SRT  X X X X
  -DVB X X X X
-Add core support for subtitle conversions for testing purposes.
-Added & documented ERROR 22000 for subtitle conversion errors.
-Need to update the UI when this is ready.
-Inspired by Issue # 32 (https://github.com/zelon88/HRConvert2/issues/32). 

--------------------
COMMIT 2/21/2023
v3.1.9 - Finish updating language packs.

-v3.1.9.
-Adjust v3.1.8 update procedure.
-Finish English language pack.
-Finish Arabic language pack.
-Work on bn, de, es, fr, hr, it, ko, pt, ru, uk, zh language packs.
-LANGUAGE PACKS COMPLETE
  -ar h,1,2,f
  -en h,1,2,f
  -bn h,1,2,f
  -de h,1,2,f
  -es h,1,2,f
  -fr h,1,2,f
  -hi h,1,2,f
  -it h,1,2,f
  -ko h,1,2,f
  -pt h,1,2,f
  -ru h,,,f
  -uk h,,,f
  -zh h,,,f

--------------------
COMMIT 1/31/2023
v3.1.8 - Add supported conversion type array to config, fix csv support, make static UI elements dynamic.

-v3.1.8.
-Remove $AllowStreams variable.
-Exposed the $SupportedConversionTypes variable in config.php.
-Fix HTML error in gui2 of each language pack.
-Fix archive conversions.
-Fix OCR operations.
-Add Csv Support.
-Create v3.1.8 UI Update Procedure.
  -No user action required, list for tracking purposes only.
  -Header
    -Update header, add dots, update date.
  -UI1
    -Update FILE INFORMATION section of header comment text.
    -Add Csv line to supported spreadsheets.
  -UI2
    -Update FILE INFORMATION section of header comment text.
    -Copy & paste && $SupportedConversionTypes condition to each button code.
    -Add Spaces.
    -Copy and paste from php start tag of file foreach to buttondiv.
    -Add csv to each spreadsheet conversion dropdown.
    -Fix javascript error on archiveconvertsubmit around line 555.
    -Check for ending /div tag.
  -Footer
    -Update header, add dots, update date.

--------------------
COMMIT 1/20/2023
v3.1.7 - Continue updating language packs.

-v3.1.7.
-Finish adding errors 13001, 13002, 13003, 13004, 13005 to docs.
-Move main loading spinner to under the cursor.
-Continuing work on updating language packs.
  -The following process can be repeated for each language pack to update them to v3.1.5.
  -Header/Footer 
    -Update "FILE INFORMATION" section of header.
  -GUI1
    -Update "FILE INFORMATION" section of header.
    -Copy/Paste to replace all file format HTML.
    -Retranslate/replace all conversion specific text.
  -GUI2
    -Update "FILE INFORMATION" section of header.
    -Update icons for media, videos.
    -Search for utility div, copy+paste lower, rename upper.
    -Append PHP counter to all loading div calls inside the file loop.
    -Replace all conversion option formats.
      -image
      -drawing
      -model
      -archive
  -LANGUAGE PACKS COMPLETE
    -ar h,1,2,f
    -en h,1,2,f
    -bn h,1,2,f
    -de h,1,2,f
    -es h,1,2,f
    -fr h.1.2.f
    -hi h,1,2,f
    -it h,1,2,f
    -ko h,1,2,f
    -pt h,1,2,f
    -ru h,1,2,f
    -uk h,1,2,f
    -zh h,1,2,f
-To Do...
  -Need to redesign/standardize PDFWork functions.
    -Utilize convert() functions instead of rewriting them.
  -Need to add csv output support.
  -Need to add default options to disable specific conversion types, similar to AllowStreams.
    -Maybe make an array of allowed types instead of separate variables?
  -Need to make arrays in config.php to store default allowed output formats.
    -This will make maintaining things easier by not having to replace the UI elements each time.
    -DisplayOutputFormats(FormatType,  DisplayType)
      -Where FormatType is the type of file and DisplayType is the type of list generated.

--------------------
COMMIT 1/20/2023
v3.1.6 - General logic updates, add error checks, improve conversion attempt procedures.

-v3.1.6.
-Improve comments.
-Add $PermissionLevels & $ApacheUser variables to verifyGlobals().
-Add a pause between conversion attempts.
-The pause is set in verifyGlobals() by $SleepTimer.
-The pause increments by 1 second every attempt.
-The max number of attempts is now set in verifyGlobals() by $StopCounter.
-Incremented values are discarded. Each conversion attempt starts with default $SleepTimer.
-Simplify image conversion process. Remove Method 1 and Method 2, now only one method to rule them all.
-Reduce number of times $SupportedConversionType is checked during a conversion.
-Removed error 8000.
-Add errors 8001, 13001,

--------------------
COMMIT 1/20/2023
v3.1.5 - Create language pack upgrade to v3.1.5 procedure.

-v3.1.5.
-Replace Leafpad with Featherpad in instructions.
-Create language pack upgrade to v3.1.5 procedure.
-Lots of changes were made to the GUI.
-This procedure is for development purposes only. No action is required by users or admins.
-This is just so I can keep track of things. 
-The following process can be repeated for each language pack to update them to v3.1.5.
-GUI1
  -Update "FILE INFORMATION" section of header.
  -Copy/Paste to replace all file format HTML.
  -Retranslate/replace all conversion specific text.
-GUI2
  -Update "FILE INFORMATION" section of header.
  -Search for utility div, copy+paste lower, rename upper.
  -Append PHP counter to all loading div calls inside the file loop.
  -Replace all conversion option formats.
-LANGUAGE PACKS COMPLETE
  -ar
  -en
-AFTER ALl LANGUAGE PACKS ARE COMPLETE
  -Test each language pack.
  -Check for HTML errors.
-Need to fix loading spinner on virus scan.

--------------------
COMMIT 1/11/2023
v3.1.2 - Improve image support. $AllowStreams config entry, bump JQuery, add some icons.

-v3.1.2.
-Improve Installation_Instructions.txt.
-Revise 3D Model support, Vector file support, Image support, Iso support.
-Improve securePath function comments, add URL support.
-Trying to automate the translation into other languages.
-Add rc-local.service template file for systems where it is missing and needs to be installed.

--------------------
COMMIT 1/10/2023
v3.1.1 - Improve image support. $AllowStreams config entry, bump JQuery, add some icons.

-v3.1.1.
-Add code to prevent stream related UI elements from displaying when the $AllowStreams config entry is set to FALSE.
-Update JQuery from v3.6.0.min to v3.6.3.min.
-Checked for Dropzone.js updates, already at latest version.
-Minor comment changes in config.php.
-Translate index files of language packs into the language of the pack.
-Add comments section containing file & application information to GUI-related files.
-Clean up array declarations, improve readibility, scalability, debugging.
-Vastly improve image support for both input and output operations.
-Added support for Heic files.
-Added support for several misc image files.
-Bumped format support count to 79 file formats.
-Adjust icons for media, videos to be unique from streams.
-Add mkisofs as a dependency, fix iso creation.
-Revise verbiage of UI elements.
-Added element specific loading spinners that are under the cursor, so less disruptive.

--------------------
COMMIT 1/4/2023
v3.1 - ScanCore to v0.9, PHP-AV v4.1.

-v3.1.
-ScanCore to v0.9, PHP-AV v4.1.
-Correct logging location, remove erroneous log directory created alongside ScanCore.
-Improve argument handling.
-Add $AllowStreams config entry to selectivly disable stream formats.

--------------------
COMMIT 1/3/2023
v3.0 - Fix syntax error, typo in config.

-v3.0.
-Fix syntax error.
-Improve config.php comments.

--------------------
COMMIT 1/3/2023
v2.9.8 & v2.9.9 - Merge pull request # 24 & 25.

-v2.9.8.
-Merge pull request # 24.
-"Added .mov to videoArray and created error"
-Pull request URL:  https://github.com/zelon88/HRConvert2/pull/24.
-Commit URL:  https://github.com/zelon88/HRConvert2/commit/cd83790bcddfbf8f97cfcefef019fede9395efdb
-Special thanks to author @HenriChabert.

-v2.9.9.
-Merge pull request # 24.
-"Add m3u8 stream support"
-Pull request URL:  https://github.com/zelon88/HRConvert2/pull/25.
-Commit URL:  https://github.com/zelon88/HRConvert2/commit/6bf64dd03caf2874075209f0a7145003e1927722
-Special thanks to author @MangoTornado.
-Updated all language packs with Stream support.

--------------------
COMMIT 1/3/2023
v2.9.7 - Merge pull request # 23.

-v2.9.7.
-Merge pull request # 23.
-"Replace spaces with _ in filename"
-Pull request URL:  https://github.com/zelon88/HRConvert2/pull/23.
-Commit URL:  https://github.com/zelon88/HRConvert2/commit/24403d8518308f8c4d303af1e9acd7945f877bd5
-Special thanks to author @HenriChabert.

--------------------
COMMIT 1/3/2023
v2.9.6 - Fix error in documentation.

-v2.9.6.
-Fix error in documentation.

--------------------
COMMIT 7/18/2022
v2.9.5 - Fix bug in OCR where non-PDF conversions would fail.

-v2.9.5.
-There was a bug in convertCore.php that lead to issue found by Github user @dwaan.
-https://github.com/dwaaan/HRConvert2-Docker/issues/6

--------------------
COMMIT 4/28/2022
v2.9.4 - Add CREATING_LANGUAGE_PACKS.txt to Documents directory.

-v2.9.4.
-Add CREATING_LANGUAGE_PACKS.txt to Documents directory.

--------------------
COMMIT 4/28/2022
v2.9.3 - Update docs, ERRO_DESCRIPTIONS.txt, add application information header to important files.

-v2.9.3.
-Improve documentation.

--------------------
COMMIT 4/28/2022
v2.9.2 - Finish adding latest core features to langauge packs.

-v2.9.2.
-Added newest features to more language packs.
  -Portuguese,
  -Russian,
  -Ukranian,
  -& Chinese.

--------------------
COMMIT 4/26/2022
v2.9.1 - Continue adding latest core features to langauge packs.

-v2.9.1.
-Added newest features to more language packs.
  -Hindu has the latest features.
  -Italian has the latest features.
  -Korean has the latest features.
-Still need to add latest features to...
  -Portuguese,
  -Russian,
  -Ukranian,
  -& Chinese.

--------------------
COMMIT 4/23/2022
v2.9 - Continue adding latest core features to language packs.

-v2.9.
-Added newest features to more language packs.
  -Arabic has the latest features.
  -Bengali has the latest features.
  -German has the latest features.
  -English has the latest features.
  -Spanish has the latest features.
  -French has the latest features.
-Still need to add latest features to...
  -Hindi,
  -Italian,
  -Korean,
  -Portuguese,
  -Russian,
  -Ukranian,
  -& Chinese.
-Adjusted ScanCore-config.php so that the Reports directory is the Logs directory.
  -It was creating empty directories that can be abused to view the servers document root depending on server config.
-If your HRConvert2 installation directory contains a subdirectory called "Reports"; it should be manually deleted.

--------------------
COMMIT 4/21/2022
v2.8.9 - Added Share File button, option to disable in config.php.

-v2.8.9.
-Added a "Share File" button to convertGui2.php for creating & copying a link to the file.
-Allows users to generate shareable URLs.
-Added $AllowUserShare in conig.php for disabling the share file feature.
-Currently the Share file feature is only avaialble in the English language pack.
-I expect to have all the language packs caught up in the next update.

--------------------
COMMIT 4/20/2022
v2.8.8 - Discovered bugs in ScanCore routine, improve User Virus Scanning.

-v2.8.8.
-Fixed the way User ScanCore Scans are called.
-Reworked the way Scan Errors & Scan Complete is determined.
-Revised logic to avoid errors during normal operation.
-Both User Scan Methods successfully detected the Eicar test virus sample.

--------------------
COMMIT 4/20/2022
v2.8.7 - Added User Virus Scanning support, download button, loading spinner options & more.

-v2.8.7.
-Fixed an undefined variable warning in the virusScan() function.
-Cleaned up memory deallocation routine in the virusScan() function.
-Only enable file related operations if valid tokens have been supplied.
-Improved sanitization integrity.
-Combined the functionality of HRScan2 into HRConvert2.
  -Now users can select to scan uploaded files for viruses on-demand using ClamAV or zelon88/ScanCore.
  -The codebases for both are "sisters" so maintainence has always been a pain.
  -Whenever one gets updated a lot of the code needed to be modified and merged into the other one.
  -Considering the major refactor that just took place with HRConvert2 (which took almmost 38 caffene fueled hours) I figure we would merge functionality instead of refactoring another core.
  -These features can be disabled via config.php.
  -Some administrators won't want users uploading potentially malicious files, which makes sense.
  -If $UserVirusScanning is enabled in config.php the HRScan2 features will be savailable.
  -If $UserVirusScanning is not enabled in config.php the HRScan2 features will be unavailable.
  -This behaviour change requires some changes to the way regular virus scanning is performed.
  -If $UserVirusScanning is enabled we must disable regular virus scanning during the file upload phase.
  -Note that all other regular virus scan operations are still enforced.
  -Meaning that if a user uploads a file that is infected, they will still NOT be allowed to perform download, conversion, archive, or OCR operations on them.
  -They WILL be allowed to scan the file using User Virus Scan options.
  -Also if a normal virus scan detects a malicious file while $AllowUserVirusScan is enabled, the file WILL NOT be immediately deleted.
  -It will be allowed to remain until regular cleanup activities remove it (after the $DeleteThreshold has passed).
-Added color specific down arrow buttons to the GUI.
-Added Download File button to convertGui2.php page.
  -Now you can redownload any file in the HRConvert2 scratch space.
-Added Scan File & Scan All buttons to convertGui2.php page.
-Added config entry for adjusting the backround color of the GUI.
-Added config entry for adjusting the loading spinner style.
-Added 6 spinners for each color scheme (24 spinners total).
-Can now specify your own spinner in config.php.
-Refactored convertGui2.php file for English & Arabic so far.
  -Need to continue doing the rest.
  -Only English & Arabic language packs have support for the new API features related to User Virus Scans.
-Updated ERROR_DESCIPTIONS.txt with the latest errors related to virus scanning.
-Fixed dropdown boxes showing reversed in Arabic language pack.
-Made dropdown arrow colors match the color scheme being used.

--------------------
COMMIT 4/19/2022
v2.8.3 - Reordered some error numbers. Finished ERROR_DESCRIPTIONS.txt.

-v2.8.3.
-Fixed Dropzone box translation not happening after updating dropzone to latest version.
-Reodered some error numbers.
-Finished adding errors and descriptions to ERROR_DESCRIPTIONS.txt.
-Took some updated screenshots.
-Saved pending changes to UI docs that should have been uploaded yesterday.
-Updated Slack Team link in README.md.

--------------------
COMMIT - 4/17/2022
v2.8.2 - Adding documentation, polishing edges.

-v2.8.2.
-"Polishing the edges" of the repo, so to speak.
-Cleaning up directory structure.
  -Added Documentation directory.
  -Moved Documentation to the documentation directory.
  -Added ERROR_DESCRIPTIONS with detailed descriptions of every error message produced in the logs.
    -Still need to add more messages.
  -Renamed How_To_Install_HRConvert.txt to INSTALLATION_INSTRUCTIONS.txt & moved to Documentation directory.
  -Moved ICON_CREDITS.txt to Documentation directory.
  -Cleaned up the comment intro section to all PHP files to have the same basic formatting & structure.
  -Polish the README some more.
  -Make logs respect the $ApplicationName variable as soon as it becomes available.
    -No more hardcoded "HRConvert2" in log entries.
  -Moved styleCore.php to Resources directory, since it only contains 4 front-end related variables and no logic of its own.
  -The goal is to cleanup the root of the repo to make it more presentable & approacable for newbies.
    -All the important files related to logic are in the root of the repo.
    -Makes the repo more modular as well.
-Make $_GET['language'] codes language case-insensitive.
-Log files are now named after the $ApplicationName variable set in config.php rather than using HRConvert2 in the filename.
-Reordered some logic.
-Disable GUI compilation when responding to a file operation request.
  -The front end doesn't use output from the core anyway.
  -This clears the way to enable such functionality by controlling the when GUI elements are actually needed.
-No longer throws 404 errors when a file conversion fails.
-No longer opens compatible files in the browser but starts a download instead.
-No more HTML undefined errors when requesitng a download.
-Added a check to verifySessionHash() function to make sure the $Salts variables are formatted as strings.

--------------------
COMMIT - 4/17/2022
v2.8.1 - Update DropZone version, update stylesheet, fix bugs.

-v2.8.1.
-Shortened some of the longer lines of code in convertCore.php.
-Fixed bugs during some operations when $VirusScan is set to TRUE in config.php.
-Update dropzone.js & dropone.css to latest release.
-Thinking about adding the option to scan with ClamAV & zelon88/ScanCore.
  -This will effectively merge the zelon88/HRScan2 project into HRConvert2.
-Thinking about adding Pell editor (port from HRCloud2 & update) to enable real-time in browser document editing.
-Going to start working on the next feature release.
  -Tick... Tock.

--------------------
COMMIT - 4/14/2022
v2.8 - Major convertCore.php refactor. Complete modular redesign. Same functionality, same look, more predictable & consistent. Easier to maintain.

-v2.8.
-Refactor the core.
  -The original codebase was developed more than 4 years ago.
    -The first unpublished experiments began in 2014.
  -The "generation" of the HRConvert2 codebase until today was "Valkyrie".
    -HonestRepair server side software comes in 3 generations. 
    -The first generation is unsafe to use. It is part of the "zelon88/HRToolkitTools" repo.
      -It was called the "Genesis" engine because it was a proof-of-concept design.
      -Hence the name "Genesis".
    -The second generation is performant & safe to use but hard to maintain.
      -It is called the "Valkyrie" engine because the runtime environment is "dynamically constructed".
      -The "Valkyrie" in Norse mythology is any of a group of maidens who serves the god Odin.
      -Valkyrie's were also the "Choosers of the slain" and decided who would die on the battlefield.
      -Because the "Valkyrie" codebase is made up of one main core served by smaller cores that dynamically call dependencies.
      -Hence the name "Valkyrie" is very fitting.
    -The third and current generation is called "Diablo". It is secure, performant, & modular.
      -"Diablo" is Spanish for Devil. 
      -The coding convention was introduced in the "Zelon88/HRCloud3" alpha repo and is currently under active development. 
      -If you're a major Cloud provider you should fear Diablo.
  -Removes most core output except from log files or when logs cannot be reached.
  -Uses a more consistent logging/error catching mechanism.
  -Makes the core extremely modular and easy to work on.
  -Adding new features is easier because logging and error functions are repeatable & consistent.
  -Output is consistent.
  -Logic flow is capable of withstanding non-fatal errors.
    -Before we stopped execution for a lot more things.
    -Logic behavior is more intuitive.
  -Errors produced use incremental error numbers that can be easily adjusted or documented.
  -Log generation happens earlier during execution, meaning more logs can be captured.
    -More logs means problems are easier to identify & debug.
  -Switching to a modern design will mean more interoperability of functions between applications.
    -It becomes easier to apply simiilar patches to other products.
  -I don't have to look at my old code anymore.
    -Programmers grow. Programming styles change. 
    -I am a better programmer now than I was when I first wrote this.
    -There will probably be bugs introduced and regressions but at least I'll enjoy working on the codebase again.
    -This work needed to get done eventually anyway considering it will be needed for HRCloud3.
-Added $Verbose config entry for controlling the amount of logging performed.
  -If $Verbose is set to TRUE every significant operation will create a log entry.
  -If $Verbose is set to FALSE only errors will create a log entry.
-$VirusScan config entry now only accepts boolean values.
-Improve formatting of config.php file for readability.
-All conversions except archive conversions now receive 5 conversion attempts.
  -Previously it was only for documents and the threshold was 10 attempts.
-Reworked the way the document conversion engine is started and verified.
-Removed a lot of unused variables.
-Started using the same capitalization scheme as HRCloud3 (Diablo style).
  -Lower case first letter variables denote highly limited scope.
  -Upper case first letter variables denote very wide (almost global) scope.
  -I know that PHP takes care of memory cleanup, and I know how variable scope works in functions.
    -I also don't care. 
    -Doing the capitalization scheme and manually NULL'ing + unsetting variables helps me keep track of variables.
    -I came up with this scheme exactly because I kept seeing dead variables in my code and I wanted it to stop.
    -This forces accountability for all variables and puts visiblity on most of them.
    -So when one of them isn't used anywhere it kind of stands out.
-Refactored Javascript a tiny bit.
-Now the core will remember when you leave and come back for a short while.
-Added unique identifier for logs. 
  -Makes searching logs much easier because requests group together.
-Core now captures the stdout of it's dependencies.
  -Writes to log if $Verbose is set to TRUE.
-Changed extraction behaviour for archive conversions.
  -Archive conversions where the destination folder already exists will have the new contents ADDED to the original archive instead of replacing them.
  -You can now build archives in this fashion, admittedly it is not for the faint of heart.
-Remove sanitizeCore.php.
  -This has been replaced by the verifyGlobals() function in convertCore.php.
  -This was a suggestion made about 5 years ago on Reddit. It finally happened!
-I worked on this commit for 30 hours straight with no sleep.
  -With only one consecutive 90 minute break to take a phone call.
  -Then I took a 3 hour nap and finished the rest, over the next 8 hours.
  -I am recovering from a broken leg and surgery to fix it and I still have a couple more weeks to go.
  -Send halp!

--------------------
COMMIT - 4/13/2022
v2.7.5 - Added 7 language translations, fixed compression bug.

-v2.7.5.
-Added code to each UI in Languages to prevent execution without the core having been loaded already.
-Fixed a bug when creating archives in .zip and .7z format.
-Touched up README.md.
-Added Russian language translation (ru).
-Added Ukranian language translation (uk).
-Added Bengali language translation (bn).
-Added German language translation (de).
-Added Korean language translation (ko).
-Added Italian language translation (it).
-Added Portugese language translation (pt).

--------------------
COMMIT - 4/13/2022
v2.7.4 - Added 4 language translations.

-v2.7.4.
-Switch from Microsoft Locale Codes to ISO 639-1 language standard.
-Updated English, French translations (en & fr).
-Added Spanish language translation (es).
-Added Chinese (Simplified) language translation (zh).
-Added Hindi language translation (hi).
-Added Arabic language translation (ar).
-Added a bunch of updated screenshots.

--------------------
COMMIT - 4/13/2022
v2.7.3 - Added multiple color schemes, French language translation, & dynamic language selection.

-v2.7.3.
-Remove winetrics dependency from installation instructions.
-Add gnuplot dependency to installation instructions.
-Consolidated some of the installation instructions.
-Added the ability to control button color via config.php.
  -Set $ButtonStyles to one of the approved options listed in config.php. 
  -If the config entry is not defined a default will be used instead (grey).
  -Supports RED, GREEN, BLUE, and GREY.
-Updated index.html files in subdirectories to the latest version from root directory.
-Moved the UI to the Languages subdirectory.
-Added French language UI.
-Added the ability to easily add more language packs.
-Added ability to control language display via $_GET request.
-Added ability to disable $_GET request language selection in config.php.
-Reworked the way the GUI is assembled to make it compatible with multiple language packs.

--------------------
COMMIT - 4/13/2022
v2.7.2 - Fixed bugs reported by a Reddit user.

-v2.7.2.
-Fix undefined variable errors in sanitizeCore.php and convertCore.php when converting PDF files.
-Fixed multiple page detection code.

--------------------
COMMIT - 4/13/2022
v2.7.1 - Add a ton of supported image formats.

-v2.7.1
-HRConvert2 uses ImageMagick to support image conversions.
-I have gone through the list of supported image formats and added some easy to support formats to expand HRConvert2's capabilities.
  -CRW
  -CIN
  -DCR
  -DDS
  -DIB
  -FLIF
  -GPLT
  -NEF
  -ORF
  -ORA
  -SCT
  -SFW
  -XCF
  -XWD
  -AVIF
  -ICO
-Also added gnuplot to the list of dependencies (to support GPLT input files).
-Fixed some supported filetype checks.
-Clean up formatting of config.php.
-Clean up some mis-indented code blocks (brackets were correct).

--------------------
COMMIT - 4/11/2022
v2.7 - v2.7 - Fix a ton of bugs, make compatible w/PHP8.

-v2.7.
-Update documentation.
  -Update installation docs to reflect latest PHP.
  -Remove mySQL requirements.
-Remove 'odf' support, change to 'odt' support.
-Specifically define $URLEcho to fix undefined variable warnings.
-Added $ShowGUI config variable for determining whether to show a full or minimal GUI.
  -Detects if the variable is set and manually defines $_GET['noGUI'].
  -Has the same function as $_GET['noGUI']. 
  -If users don't have this variable set in their config.php file then a full GUI will be shown.
-HRConvert2 looks best when you use it in an iframe on your website.
-Fixed some if/while/foreach statements not having whitespace before the opening '('.
-Removed reference to $UserID in logs.
-Verified that all PDFWork is capable of functioning.
-Verified that all document conversions are capable of functioning.
-Reworked the way archives are extracted & compressed.
-Improved logging during dearchiving.
-Added $ApplicationName & $ApplicationTitle config entries in config.php.
  -If these are not set; defaults are used instead.
-Upgraded to JQuery 3.6.0 from 3.3.1.
  -Remove 3.3.1 & 3.1.0 legacy versions as they are no longer required.
-Added $ShowFinePrint config entry to config.php for showing or hiding the TOS & PP links.
  -If this is not set; a default is used instead.
-Fixed noGUI mode so that the divs are centered & the produced HTML is valid.
-Cleaned up some sloppy assignments in dearch/rearch code.
-Move temporary archive location to ConvertDir from ConvertTempDir.
-Improved consistency of UI elements.
-Switched dearchive code from using the 7z e switch to the 7z x switch to preserve paths.
-Fixed a bug in rar archive output paths.
-Changed the rar archiving arguments from "rar a -ep " to "rar a -ep1 -r" to preserve folder structure.
-Verified that all extraction / rearchiving operations work.
-Remove $Thorough & $HighPerformanceAV from config.php as they were no longer applicable.
-Reworked the way sanitizeCore.php converts strings into arrays.
-Changed $VirusScan config entry default value from '1' to TRUE.
  -The core will still recognize '1' as a valid entry to support legacy config files.
-Remove tar.bz2 option from archiveAll menu.
-Rework the way archiveAll feature is accomplished.
-Improve logging consistencies.
-Remove old iPhone code from index.html.
  -This is easier to accomplish with screen size anyway.

--------------------
COMMIT - 2/11/2020
v2.6 - Improve sanitization per Issue #4.

-v2.6.
-Improve sanitization per Issue #4.
-Github user @robre pointed out some typos in sanitizeCore.php, specifically lines 95 & line 96 from v2.4.
-These bugs mean to me that I need to review more of this code.
-Thank you, @robre!

--------------------
COMMIT 11/18/2018
v2.4 - Improve sanitization.

-v2.4.
-Improve sanitization.
-Based on input from neogeovr on Reddit.

--------------------
COMMIT 10/23/2018
v2.3 - Add support for m4a, m4p, m4v files.

-v2.3.
-Add support for m4a, m4p, and m4v input files.

--------------------
COMMIT 9/25/2018
v2.2 - Fix typos in comments.

-v2.2.
-Fix typos in comments.

--------------------
COMMIT 7/22/2018
v2.1 - Fix occasional warnings during file cleanup.

-v2.1.
-Fix occasional warnings.

----------
COMMIT 7/20/2018
v2.0 - Polish some rough edges in the back end.

-v2.0.
-Consolidate some code in similar fashion to the recent HRC2 refactor.
-Now when no extension is specified a default will be used instead of displaying a 404 error.
-Tweak auto-cleaner to make it work in a safer, more reliable way.
-Add conversion and archive support for tar.bz2.
-Now when bulk archives are created any converted files will be included in the archive as well as the originals.
-Fix the way files sync between locations and the way symlinks are controlled.

----------
COMMIT 7/8/2018
v1.9 - Improve security.

-v1.9.
-Added checks for dangerous files to all Convert operations and to the GUI.
-Fix PDF files showing the image options button when there is no div to display.
-Added this changelog to keep track of thoughts, changes, and ideas.
----------
